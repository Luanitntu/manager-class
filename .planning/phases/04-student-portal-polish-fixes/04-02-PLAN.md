---
phase: 04-student-portal-polish-fixes
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - client/app/pages/dashboard.vue
  - client/app/pages/documents.vue
  - client/app/pages/payments.vue
  - client/app/composables/useDocuments.ts
  - client/app/composables/usePayments.ts
autonomous: true
requirements: [UI-02, DATA-04, DATA-05, BUG-05]
user_setup: []
must_haves:
  truths:
    - "D-01: Student dashboard uses overview plus sections layout, not stats-only and not a single today-only panel."
    - "D-02: Student dashboard section order is Schedule, Documents, Scores, Payments."
    - "D-04: Student dashboard sections have action-oriented empty states and are not hidden when empty."
    - "D-05/D-06: Scores/comments appear as dashboard previews only, latest 3 scores and latest 3 comments."
    - "D-08: Student comments render as teacher feedback/progress timeline with category, author, date, and content."
    - "D-09: `/documents` and `/payments` remain shared routes, branched by role."
    - "D-10: Student documents render material cards with type icon, title, category, assignment context when available, and Open/Download actions."
    - "D-11: Student payments render account-style tuition cards with class, totals, due date, status chip, and history access."
    - "D-12: Teacher-only helper queries such as `useStudents()` and `useClasses()` are gated by role before query execution."
  artifacts:
    - path: "client/app/pages/dashboard.vue"
      provides: "Student portal dashboard sections"
    - path: "client/app/pages/documents.vue"
      provides: "Role-branched student document cards"
    - path: "client/app/pages/payments.vue"
      provides: "Role-branched student tuition account view"
---

<objective>
Polish the student portal UI and close data-display bugs by wiring student dashboard sections, student-focused documents, and student-focused payments through role-safe composables and distinct loading/empty/error states.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-student-portal-polish-fixes/04-CONTEXT.md
@.planning/phases/04-student-portal-polish-fixes/04-RESEARCH.md
@.planning/phases/02-visual-system-app-shell-refresh/02-UI-SPEC.md
@client/app/pages/dashboard.vue
@client/app/pages/documents.vue
@client/app/pages/payments.vue
@client/app/components/AppPageHeader.vue
@client/app/components/AppState.vue
@client/app/composables/useDashboard.ts
@client/app/composables/useDocuments.ts
@client/app/composables/usePayments.ts
@client/app/composables/useStudents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build student dashboard sections</name>
  <files>client/app/pages/dashboard.vue</files>
  <read_first>client/app/pages/dashboard.vue, client/app/composables/useDashboard.ts, client/app/composables/useDocuments.ts, client/app/composables/usePayments.ts, client/app/composables/useStudents.ts</read_first>
  <action>Branch the dashboard by `data.role === 'STUDENT'`. Keep existing overview stat cards, then render sections in exact order: Schedule, Documents, Scores, Payments. Use `useDocuments`, `useTuitions`, `useMyScores`, and `useMyComments` for targeted queries. Show latest 3 scores and latest 3 comments. Use `AppState` or compact section-specific state blocks so loading, empty, error, and no-permission states remain distinct.</action>
  <verify>rg -n "Schedule|Documents|Scores|Payments|useMyScores|useMyComments" client/app/pages/dashboard.vue</verify>
  <acceptance_criteria>
    - Student dashboard summarizes own upcoming sessions, documents, scores/comments, and payments.
    - Section order is Schedule, Documents, Scores, Payments.
    - Empty sections remain visible and explain the absence of data.
  </acceptance_criteria>
  <done>Student dashboard is a portal-style overview with required sections.</done>
</task>

<task type="auto">
  <name>Task 2: Gate teacher-only document helper queries</name>
  <files>client/app/pages/documents.vue</files>
  <read_first>client/app/pages/documents.vue, client/app/composables/useClasses.ts, client/app/composables/useStudents.ts</read_first>
  <action>Prevent `useClasses()` and `useStudents()` queries from running for student actors. If the existing composables do not support `enabled`, add optional `enabled` arguments conservatively or restructure the page so teacher-only selection data is queried only in a teacher/assistant management component. Preserve teacher create/upload/assign/delete behavior.</action>
  <verify>rg -n "canManage|enabled|useClasses|useStudents" client/app/pages/documents.vue client/app/composables/useClasses.ts client/app/composables/useStudents.ts</verify>
  <acceptance_criteria>
    - Student document page does not call teacher-only students/classes endpoints.
    - Teacher/assistant document management still has class/student assignment options where allowed.
  </acceptance_criteria>
  <done>Document helper queries are role-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Render student material cards</name>
  <files>client/app/pages/documents.vue, client/app/composables/useDocuments.ts</files>
  <read_first>client/app/pages/documents.vue, client/app/composables/useDocuments.ts</read_first>
  <action>For student actors, render a read-only card/list view with type icon, title, category, assignment context when available from API fields, and Open/Download actions. Use student-specific empty copy: `No shared documents yet. Your teacher's materials will appear here when assigned.` Keep management controls hidden for students.</action>
  <verify>rg -n "No shared documents|Open|Download|canManage" client/app/pages/documents.vue</verify>
  <acceptance_criteria>
    - Student document cards are read-only and action-focused.
    - Link materials open in a new tab; file materials use the existing download endpoint.
    - Teacher-only controls never render for students.
  </acceptance_criteria>
  <done>Student document UI is trustworthy and role-focused.</done>
</task>

<task type="auto">
  <name>Task 4: Gate teacher-only payment helper queries</name>
  <files>client/app/pages/payments.vue</files>
  <read_first>client/app/pages/payments.vue, client/app/composables/useClasses.ts, client/app/composables/useStudents.ts</read_first>
  <action>Prevent `useClasses()` and `useStudents()` from executing for student actors on `/payments`. Ensure create tuition, record payment, and send reminder controls are teacher-only. Keep `useTuitions()` available to students because the backend scopes it to the authenticated student.</action>
  <verify>rg -n "canManage|useClasses|useStudents|New Tuition|Record|Send reminder" client/app/pages/payments.vue</verify>
  <acceptance_criteria>
    - Student payments page does not call teacher-only endpoints.
    - Teacher payment management still functions.
  </acceptance_criteria>
  <done>Payment helper queries are role-safe.</done>
</task>

<task type="auto">
  <name>Task 5: Render student tuition account cards and payment history</name>
  <files>client/app/pages/payments.vue, client/app/composables/usePayments.ts</files>
  <read_first>client/app/pages/payments.vue, client/app/composables/usePayments.ts</read_first>
  <action>For student actors, replace the admin table with account-style tuition cards: class, total/paid/remaining, due date, status chip, and a `View history` action that opens the existing detail dialog in read-only mode. Ensure empty state says no payments yet, not create a tuition bill. Payment history should show receipt, method, date, amount, and no-history state.</action>
  <verify>rg -n "View history|remaining|Payment history|No payments yet" client/app/pages/payments.vue</verify>
  <acceptance_criteria>
    - Student payment status and history are understandable from the student view.
    - Student cannot record payments or send reminders.
    - BUG-05 is addressed for student-facing status/history display.
  </acceptance_criteria>
  <done>Student payments are displayed as an account view with history.</done>
</task>

</tasks>

<verification>
- From `client/`, run `npm run typecheck` after frontend changes.
- From `client/`, run `npm run lint` and `npm run build` if Node/npm are available.
- Manual smoke: login as the demo student, open `/dashboard`, `/documents`, and `/payments`, and confirm no teacher-only endpoint errors appear in console/network.
</verification>

<success_criteria>
- Student dashboard shows classes/schedule, documents, scores/comments, and payments from targeted queries.
- Student document and payment pages no longer appear empty or broken because of teacher-only helper query failures.
- Empty/loading/error states are distinct and role-appropriate.
- Payment status/history display is clear in the student view.
</success_criteria>

<output>
Create `.planning/phases/04-student-portal-polish-fixes/04-02-SUMMARY.md` after this plan completes.
</output>
