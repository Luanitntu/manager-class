---
phase: 04-student-portal-polish-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/modules/student/student.controller.ts
  - server/src/modules/student/student.service.ts
  - server/src/modules/student/student.repository.ts
  - server/src/modules/student/student.service.spec.ts
  - client/app/composables/useStudents.ts
autonomous: true
requirements: [UI-02, DATA-04, DATA-05, BUG-05]
user_setup: []
must_haves:
  truths:
    - "D-03: Student dashboard must use targeted queries for documents, payments, and scores/comments instead of bloating /dashboard."
    - "D-07: Add student self endpoints/composables for scores and comments so the frontend does not need to know its own user id."
    - "Student-owned score/comment reads must preserve tenant isolation and must not expose another student's records."
    - "Backend changes must keep Controller -> Service -> Repository boundaries where existing repository helpers fit."
  artifacts:
    - path: "client/app/composables/useStudents.ts"
      provides: "Self-scoped score/comment query composables for student dashboard previews"
---

<objective>
Create the backend and composable foundation for student-owned scores/comments so the student dashboard can show latest score and teacher feedback previews without knowing or passing the student's own id.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-student-portal-polish-fixes/04-CONTEXT.md
@.planning/phases/04-student-portal-polish-fixes/04-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@server/src/modules/student/student.controller.ts
@server/src/modules/student/student.service.ts
@server/src/modules/student/student.repository.ts
@client/app/composables/useStudents.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add self-scoped score/comment API endpoints</name>
  <files>server/src/modules/student/student.controller.ts, server/src/modules/student/student.service.ts</files>
  <read_first>server/src/modules/student/student.controller.ts, server/src/modules/student/student.service.ts, server/src/modules/student/student.repository.ts</read_first>
  <action>Add `GET /students/me/scores` and `GET /students/me/comments` before parameterized `:id` routes so `me` is not parsed as a UUID. Restrict these routes to student actors if existing role decorators allow it, or enforce `actor.role === STUDENT` in the service. Implement service methods that list records for `actor.id` and resolve teacher scope from `actor.tenantId` or the student's `teacherId` row, without allowing arbitrary student ids.</action>
  <verify>rg -n "me/scores|me/comments" server/src/modules/student</verify>
  <acceptance_criteria>
    - `/students/me/scores` returns only the authenticated student's scores.
    - `/students/me/comments` returns only the authenticated student's comments.
    - The parameterized `/:id` routes still work for teachers and authorized actors.
  </acceptance_criteria>
  <done>Self-scoped student score/comment endpoints exist and are ordered safely before UUID routes.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve tenant isolation in score/comment service methods</name>
  <files>server/src/modules/student/student.service.ts, server/src/modules/student/student.repository.ts</files>
  <read_first>server/src/modules/student/student.service.ts, server/src/modules/student/student.repository.ts</read_first>
  <action>Audit `listScores` and `listComments` for student actors. If `teacherScope(actor)` can fail for students, refactor through a small helper such as `resolveStudentTeacherId(actor, studentId)` that confirms the student belongs to the teacher before repository reads. Keep teacher and assistant access behavior unchanged.</action>
  <verify>rg -n "resolveStudentTeacherId|listScores|listComments" server/src/modules/student/student.service.ts</verify>
  <acceptance_criteria>
    - Student reads do not depend on a missing tenant context if the student row has `teacherId`.
    - Teacher reads are still filtered by tenant id.
    - Cross-student reads still throw `ForbiddenException` or `NotFoundException`.
  </acceptance_criteria>
  <done>Score/comment reads are tenant-safe for both student self access and teacher access.</done>
</task>

<task type="auto">
  <name>Task 3: Add frontend self query composables</name>
  <files>client/app/composables/useStudents.ts</files>
  <read_first>client/app/composables/useStudents.ts, client/app/composables/useDashboard.ts</read_first>
  <action>Add `useMyScores()` and `useMyComments()` composables that call `/students/me/scores` and `/students/me/comments` via `useApi().request`, with stable query keys such as `['student-me-scores']` and `['student-me-comments']`. Keep existing teacher-facing `useStudentScores(id)` and `useStudentComments(id)` unchanged.</action>
  <verify>rg -n "useMyScores|useMyComments|students/me" client/app/composables/useStudents.ts</verify>
  <acceptance_criteria>
    - Student dashboard can fetch score/comment previews without knowing auth user id.
    - Teacher-facing student detail flows still use the existing id-based composables.
  </acceptance_criteria>
  <done>Student self score/comment composables are available.</done>
</task>

<task type="auto">
  <name>Task 4: Add focused backend tests for self access</name>
  <files>server/src/modules/student/student.service.spec.ts</files>
  <read_first>server/src/modules/session/session-conflict.service.spec.ts, server/src/modules/student/student.service.ts</read_first>
  <action>Create or extend a Jest unit spec for `StudentService` using mocked repository/Prisma methods. Cover: a student can list own scores/comments, a student cannot list another student's records, teacher tenant reads pass the tenant id, and missing tenant/student teacher scope fails closed.</action>
  <verify>cd server; npm test -- student.service.spec.ts</verify>
  <acceptance_criteria>
    - Tests cover self endpoints' service-level access rules.
    - Tests do not require a real database.
  </acceptance_criteria>
  <done>Student self score/comment access has regression coverage.</done>
</task>

</tasks>

<verification>
- From `server/`, run `npm test -- student.service.spec.ts`.
- If available, run `npm run lint` and `npm run build` from `server/`.
- From `client/`, run `npm run typecheck` after composable changes.
</verification>

<success_criteria>
- Student self score/comment endpoints exist and are secure.
- Student self composables exist and use feature composable patterns.
- Backend service tests cover role/scope behavior for the new self reads.
</success_criteria>

<output>
Create `.planning/phases/04-student-portal-polish-fixes/04-01-SUMMARY.md` after this plan completes.
</output>
