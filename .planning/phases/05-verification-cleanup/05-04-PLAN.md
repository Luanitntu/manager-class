---
phase: 05-verification-cleanup
plan: 04
type: execute
wave: 4
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - .planning/phases/05-verification-cleanup/05-VERIFICATION.md
autonomous: true
requirements:
  - VER-01
  - VER-02
  - VER-03
  - VER-04
  - VER-05
  - VER-06
must_haves:
  truths:
    - "Frontend lint passes after Phase 5 cleanup."
    - "Frontend typecheck passes after Phase 5 cleanup."
    - "Frontend build passes after Phase 5 cleanup."
    - "Final verification record consolidates commands, exit codes, scan evidence, visual QA, cleanup, deferred markers, and backend-check decision."
  artifacts:
    - path: ".planning/phases/05-verification-cleanup/05-VERIFICATION.md"
      provides: "Final Phase 5 verification and cleanup record"
  key_links:
    - from: ".planning/phases/05-verification-cleanup/05-STATIC-SCANS.md"
      to: ".planning/phases/05-verification-cleanup/05-VERIFICATION.md"
      via: "Static scan summary and deferred marker classifications"
      pattern: "Static Scans"
    - from: ".planning/phases/05-verification-cleanup/05-UAT.md"
      to: ".planning/phases/05-verification-cleanup/05-VERIFICATION.md"
      via: "Manual smoke/visual QA summary"
      pattern: "Human Verification"
---

## Phase Goal

**As a** release owner, **I want to** close Phase 5 with passing gates and a complete verification record, **so that** v1.1 can be accepted as stable.

<objective>
Run final frontend gates and consolidate Phase 5 evidence into the verification record.

Purpose: Satisfy VER-01 through VER-06 and produce the release-grade proof artifact.
Output: `05-VERIFICATION.md`.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-verification-cleanup/05-UI-SPEC.md
@.planning/phases/05-verification-cleanup/05-STATIC-SCANS.md
@.planning/phases/05-verification-cleanup/05-CLEANUP.md
@.planning/phases/05-verification-cleanup/05-SMOKE-QA.md
@.planning/phases/05-verification-cleanup/05-UAT.md
</context>

## Artifacts this phase produces

- `.planning/phases/05-verification-cleanup/05-VERIFICATION.md`

<tasks>

<task type="auto">
  <name>Task 1: Run final frontend gates from client</name>
  <read_first>
    - `client/package.json`
    - `client/nuxt.config.ts`
    - `.planning/phases/05-verification-cleanup/05-CLEANUP.md`
  </read_first>
  <action>From `client/`, run `npm run lint`, `npm run typecheck`, and `npm run build` in that order. Record command, working directory, start/end time if available, exit code, and important warnings/errors in `05-VERIFICATION.md`. Do not run backend commands unless `git status --short` shows files under `server/`; when no backend files changed, record the exact statement `Backend checks not run; no backend files changed.` for VER-06.</action>
  <verify>
    <automated>cd client; npm run lint</automated>
    <automated>cd client; npm run typecheck</automated>
    <automated>cd client; npm run build</automated>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/05-verification-cleanup/05-VERIFICATION.md'; if(!(Test-Path $p)){ Write-Error 'Missing 05-VERIFICATION.md'; exit 1 }; $changed=& git status --short; if($changed -notmatch ' server/'){ $text=Get-Content -Raw $p; if($text -notmatch [regex]::Escape('Backend checks not run; no backend files changed.')){ Write-Error 'Missing exact VER-06 no-backend decision'; exit 1 } }; exit 0"</automated>
  </verify>
  <acceptance_criteria>VER-01, VER-02, and VER-03 commands are run and recorded with exit codes; VER-06 backend decision is recorded exactly when no backend files changed.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Consolidate Phase 5 verification and cleanup record</name>
  <read_first>
    - `.planning/phases/05-verification-cleanup/05-STATIC-SCANS.md`
    - `.planning/phases/05-verification-cleanup/05-CLEANUP.md`
    - `.planning/phases/05-verification-cleanup/05-SMOKE-QA.md`
    - `.planning/phases/05-verification-cleanup/05-UAT.md`
  </read_first>
  <action>Complete `05-VERIFICATION.md` with sections: Automated Checks; Static Scans; Smoke Routes; Visual QA Evidence; Cleanup Files Touched/Removed; Deferred Markers; Backend Check Decision; Requirement Coverage for VER-01 through VER-06; Residual Risks; Result. Include paths to screenshots or exact screenshot blockers. Include all files touched/removed from `05-CLEANUP.md`. Include the deferred admin/center/general marker classifications from `05-STATIC-SCANS.md`. Mark Phase 5 passed only if lint, typecheck, build, target smoke scan, UI-kit boundary scan, package/config scan, smoke QA, and UAT have no blocking issues.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/05-verification-cleanup/05-VERIFICATION.md'; if(!(Test-Path $p)){ Write-Error 'Missing 05-VERIFICATION.md'; exit 1 }; $text=Get-Content -Raw $p; foreach($item in @('VER-01','VER-02','VER-03','VER-04','VER-05','VER-06','Automated Checks','Static Scans','Smoke Routes','Visual QA Evidence','Cleanup Files Touched/Removed','Deferred Markers','Backend Check Decision','Requirement Coverage','Residual Risks','Result')){ if($text -notmatch [regex]::Escape($item)){ Write-Error \"Missing verification evidence marker: $item\"; exit 1 } }; foreach($req in @('VER-01','VER-02','VER-03','VER-04','VER-05','VER-06')){ if($text -notmatch \"(?is)$req.*(PASS|FAIL|BLOCKED)\"){ Write-Error \"Missing PASS/FAIL/BLOCKED evidence for $req\"; exit 1 } }"</automated>
  </verify>
  <acceptance_criteria>`05-VERIFICATION.md` is the single final record for Phase 5; every VER-01..VER-06 item has PASS/FAIL/BLOCKED evidence; deferred markers and backend decision are explicit; no schema push task or backend verification is added unless backend files changed.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local command execution -> release record | Final gates must be accurately recorded with exit codes. |
| frontend-only phase -> backend verification decision | Backend checks must run only when backend files changed, otherwise exact no-backend statement is required. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-07 | Repudiation | final command results | mitigate | Record command, working directory, exit code, and warnings/errors in `05-VERIFICATION.md`. |
| T-05-08 | Tampering | verification summary | mitigate | Link final status back to `05-STATIC-SCANS.md`, `05-SMOKE-QA.md`, `05-UAT.md`, and command outputs. |
| T-05-SC | Tampering | package installs | accept | No installs or new UI dependencies are allowed in final verification. |
</threat_model>

<source_audit>
GOAL covered: complete stability proof for Tailwind migration.
REQ covered: VER-01, VER-02, VER-03, VER-04, VER-05, VER-06.
RESEARCH covered: no RESEARCH.md; UI-SPEC Verification Contract used.
CONTEXT covered: no Phase 5 CONTEXT.md; project/roadmap decisions honored.
Schema gate: no schema-relevant files planned; no DB push task.
</source_audit>

<verification>
Overall Phase 5 checks: `npm run lint`, `npm run typecheck`, `npm run build` from `client/`; static scans; smoke/visual QA evidence; backend check decision.
</verification>

<success_criteria>
Phase 5 passes when `05-VERIFICATION.md` records passing frontend gates, passing target static scans, completed desktop/mobile smoke evidence, completed human visual QA, cleanup proof, explicit deferred marker classification, and the exact backend check decision.
</success_criteria>

<output>
Create `.planning/phases/05-verification-cleanup/05-04-SUMMARY.md` when done.
</output>
