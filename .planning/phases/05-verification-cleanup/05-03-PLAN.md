---
phase: 05-verification-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - .planning/phases/05-verification-cleanup/05-UAT.md
autonomous: false
requirements:
  - VER-04
  - VER-05
must_haves:
  truths:
    - "A human has verified or explicitly blocked the Phase 5 smoke targets on desktop and mobile-width viewports."
    - "Manual QA evidence covers app shell navigation, calendar, priority routes, and visual regression criteria."
    - "Any issue is written with exact route, viewport, symptom, severity, and next action."
  artifacts:
    - path: ".planning/phases/05-verification-cleanup/05-UAT.md"
      provides: "Human smoke and visual QA result record"
  key_links:
    - from: ".planning/phases/05-verification-cleanup/05-SMOKE-QA.md"
      to: ".planning/phases/05-verification-cleanup/05-UAT.md"
      via: "Human verifies automated/source evidence and records pass/issues"
      pattern: "VER-04"
---

## Phase Goal

**As a** release owner, **I want to** manually confirm the visible migration outcome, **so that** Phase 5 does not rely only on static checks.

<objective>
Run a blocking human smoke/visual QA checkpoint for Phase 5 targets.

Purpose: Satisfy VER-04 and VER-05 manual evidence requirements.
Output: `05-UAT.md` with pass/fail, notes, blockers, and issue table.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-verification-cleanup/05-UI-SPEC.md
@.planning/phases/05-verification-cleanup/05-SMOKE-QA.md
@.planning/phases/04-priority-old-page-redesign/04-UAT.md
</context>

## Artifacts this phase produces

- `.planning/phases/05-verification-cleanup/05-UAT.md`

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human smoke and visual QA checkpoint</name>
  <read_first>
    - `.planning/phases/05-verification-cleanup/05-UI-SPEC.md`
    - `.planning/phases/05-verification-cleanup/05-SMOKE-QA.md`
  </read_first>
  <action>Ask the user to verify the running app as an authenticated teacher at desktop width near 1440px and mobile width near 390px. Required targets: shell navigation/profile menu/profile dialog; `/calendar` month/week, today/previous/next, create/open session dialog, contained board overflow; `/assistants` loading/populated/empty/search/create/pagination/mobile wrapping; `/assistants/[id]` header/profile/salary/classes/tabs/tables/edit dialog/mobile stacking; `/audit-logs` filters/clear filters/badges/table/empty/pagination/mobile table overflow; `/profile` hero/metrics/profile editor/session details/mobile stacking. Create `05-UAT.md` with each target marked pass, issue, skipped, or blocked; include route, viewport, browser, screenshot reference if available, and whether there is any broken UI, overflow, overlap, clipped controls, unreadable text, missing states, or unintended visual regression.</action>
  <verify>
    <human-check>Type `approved` only if every required target passes, or describe issues with route, viewport, and symptom.</human-check>
    <automated>powershell -NoProfile -Command "$p='.planning/phases/05-verification-cleanup/05-UAT.md'; if(!(Test-Path $p)){ Write-Error 'Missing 05-UAT.md'; exit 1 }; $text=Get-Content -Raw $p; foreach($item in @('Shell','/calendar','/assistants','/assistants/[id]','/audit-logs','/profile','desktop','mobile','VER-04','VER-05')){ if($text -notmatch [regex]::Escape($item)){ Write-Error \"Missing UAT evidence marker: $item\"; exit 1 } }; if($text -notmatch '(approved|PASS|pass|issue|blocked|BLOCKER)'){ Write-Error 'Missing human result disposition'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>`05-UAT.md` exists; every VER-04 route and VER-05 visual condition has a human result; any issue has exact route/viewport/symptom/severity; execution does not continue to final verification if blocking issues remain.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| human browser session -> UAT record | Manual observations become release evidence and must be precise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-05-05 | Repudiation | `05-UAT.md` | mitigate | Require route, viewport, result, and issue detail for every target before approval. |
| T-05-06 | Information Disclosure | screenshots/UAT notes | mitigate | Do not paste secrets, tokens, or private student data into UAT notes; describe UI state generically when needed. |
| T-05-SC | Tampering | package installs | accept | No package-manager install task exists. |
</threat_model>

<source_audit>
GOAL covered: manual proof of stable migration.
REQ covered: VER-04, VER-05.
RESEARCH covered: no RESEARCH.md; UI-SPEC Visual QA Contract used.
CONTEXT covered: no Phase 5 CONTEXT.md; Phase 4 UAT acceptance used as prior evidence.
</source_audit>

<verification>
Plan 03 is complete when human smoke QA is approved or blocking issues are recorded for gap closure.
</verification>

<success_criteria>
Manual evidence confirms no broken UI, overflow, overlap, clipped controls, unreadable text, missing states, or unintended visual regression in the required Phase 5 smoke scope.
</success_criteria>

<output>
Create `.planning/phases/05-verification-cleanup/05-03-SUMMARY.md` when done.
</output>
