---
phase: 02-tailwind-design-system-shared-ui-kit
plan: 06
type: execute
wave: 4
depends_on:
  - 02-03
  - 02-04
  - 02-05
files_modified:
  - client/app/components/AppSkeleton.vue
  - client/app/components/AppToast.vue
  - client/app/components/ClassLocation.vue
autonomous: true
requirements:
  - UIKIT-04
  - UIKIT-05
must_haves:
  truths:
    - "`AppSkeleton` public props and variants continue working while styling delegates to `UiSkeleton`."
    - "`AppToast` keeps `useToast`, roles, dismiss behavior, stacking, and desktop/mobile placement while using `UiToast` and Tailwind utilities."
    - "`ClassLocation` no longer uses `v-icon` or `v-chip` and preserves inline/chip modes, meeting links, stop propagation, and labels."
    - "Touched proof surfaces become Tailwind-only and visually parity-checked on desktop and mobile widths."
  prohibitions:
    - "No broad page rewrites, no high-risk dialog migrations, and no changes to feature composables."
  artifacts:
    - path: "client/app/components/AppSkeleton.vue"
      provides: "Compatibility wrapper over UiSkeleton"
    - path: "client/app/components/AppToast.vue"
      provides: "App singleton toast stack using UiToast"
    - path: "client/app/components/ClassLocation.vue"
      provides: "Vuetify-free location chip/inline display using UiBadge and AppIcon"
  key_links:
    - from: "client/app/components/AppSkeleton.vue"
      to: "client/app/components/ui/UiSkeleton.vue"
      via: "Delegates existing props to UiSkeleton"
      pattern: "UiSkeleton"
    - from: "client/app/components/AppToast.vue"
      to: "client/app/components/ui/UiToast.vue"
      via: "Renders toast items through UiToast"
      pattern: "UiToast"
    - from: "client/app/components/ClassLocation.vue"
      to: "client/app/components/ui/UiBadge.vue"
      via: "Default chip mode uses UiBadge"
      pattern: "UiBadge"
---

## Phase Goal

**As a** developer, **I want to** build app pages using shared Tailwind UI components, **so that** teacher and student screens stay consistent without repeated Vuetify-era markup.

<objective>
Prove the shared UI kit by migrating three small shared surfaces with stable public behavior.

Purpose: Satisfy UIKIT-05 and visible portions of UIKIT-04 by replacing practical duplicated/legacy shared patterns per D-16 and D-17 while preserving parity per D-05 through D-07.
Output: Tailwind-only `AppSkeleton`, `AppToast`, and `ClassLocation` integrations.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-CONTEXT.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-RESEARCH.md
@client/app/components/AppSkeleton.vue
@client/app/components/AppToast.vue
@client/app/components/ClassLocation.vue
@client/app/components/ui/UiSkeleton.vue
@client/app/components/ui/UiToast.vue
@client/app/components/ui/UiBadge.vue
@client/app/components/AppIcon.vue
@client/app/components/SessionDialog.vue
@client/app/pages/assistants/index.vue
@client/app/pages/audit-logs.vue
@client/app/pages/profile.vue
</context>

## Artifacts this phase produces

- Updated `client/app/components/AppSkeleton.vue`
- Updated `client/app/components/AppToast.vue`
- Updated `client/app/components/ClassLocation.vue`
- Existing symbols preserved: `AppSkeleton`, `AppToast`, `ClassLocation`
- New proof links used: `UiSkeleton`, `UiToast`, `UiBadge`, `AppIcon`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate AppSkeleton wrapper to UiSkeleton</name>
  <files>client/app/components/AppSkeleton.vue</files>
  <read_first>
    - client/app/components/AppSkeleton.vue
    - client/app/components/ui/UiSkeleton.vue
    - client/app/pages/dashboard.vue
    - client/app/pages/profile.vue
    - client/app/components/SessionDialog.vue
    - .planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Existing `AppSkeleton` props remain `variant`, `rows`, `columns`, and `cards`.
    - Existing variants remain `dashboard`, `stats`, `grid`, `table`, `list`, `calendar`, `form`, and `detail`.
    - All component-scoped CSS is removed from `AppSkeleton.vue`.
  </behavior>
  <action>Convert `AppSkeleton.vue` into a compatibility wrapper that passes existing props to `UiSkeleton`. Preserve current public prop defaults and variant names because many pages import `AppSkeleton`. Remove its scoped CSS and do not alter call sites.</action>
  <acceptance_criteria>
    - `AppSkeleton.vue` has no `<style>` block.
    - `AppSkeleton.vue` still defines the same prop names/defaults or an equivalent typed wrapper contract.
    - Existing usages compile without call-site edits.
  </acceptance_criteria>
  <verify>
    <automated>$lt=[char]60; $bad = rg -n "(($lt)style|($lt)v-|($lt)/v-|lang=[`"']scss[`"']|\.scss|@use|@forward)" client/app/components/AppSkeleton.vue; if ($bad) { $bad; exit 1 }</automated>
    <automated>rg -n "UiSkeleton|dashboard|stats|grid|table|list|calendar|form|detail" client/app/components/AppSkeleton.vue</automated>
    <automated>npm.cmd --prefix client run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate AppToast stack to Tailwind and UiToast</name>
  <files>client/app/components/AppToast.vue</files>
  <read_first>
    - client/app/components/AppToast.vue
    - client/app/components/ui/UiToast.vue
    - client/app/app.vue
    - client/app/composables/useToast.ts
    - .planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
  </read_first>
  <behavior>
    - `AppToast` still uses `useToast()` for `toasts` and `dismiss`.
    - Error toasts keep `role="alert"` and non-error toasts keep `role="status"` through `UiToast`.
    - Desktop position remains bottom-right; mobile position remains bottom inset/full width.
    - TransitionGroup behavior remains present or equivalent.
  </behavior>
  <action>Replace `AppToast.vue` scoped CSS and item markup with Tailwind utilities and `UiToast` item rendering. Preserve app singleton behavior, toast type icons, dismiss callback, aria live region, roles, stacking, desktop/mobile placement, and reduced-motion intent. Do not change `useToast` or `client/app/app.vue`.</action>
  <acceptance_criteria>
    - `AppToast.vue` contains `UiToast`.
    - `AppToast.vue` has no `<style>` block and no Vuetify tags.
    - Toast roles, dismiss button, and live region remain source-visible.
  </acceptance_criteria>
  <verify>
    <automated>$lt=[char]60; $bad = rg -n "(($lt)style|($lt)v-|($lt)/v-|lang=[`"']scss[`"']|\.scss|@use|@forward)" client/app/components/AppToast.vue; if ($bad) { $bad; exit 1 }</automated>
    <automated>rg -n "UiToast|aria-live|dismiss|role|TransitionGroup|bottom" client/app/components/AppToast.vue</automated>
    <automated>npm.cmd --prefix client run lint</automated>
    <automated>npm.cmd --prefix client run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace ClassLocation Vuetify chip/icon usage</name>
  <files>client/app/components/ClassLocation.vue</files>
  <read_first>
    - client/app/components/ClassLocation.vue
    - client/app/components/ui/UiBadge.vue
    - client/app/components/AppIcon.vue
    - client/app/components/SessionDialog.vue
    - client/app/pages/classes/index.vue
    - .planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Inline mode renders icon plus text/link and preserves `@click.stop`.
    - Default mode renders a badge/chip equivalent and preserves `size`, link target, and label behavior.
    - Meeting links keep `target="_blank"` and `rel="noopener"`.
    - No `v-icon` or `v-chip` remains.
  </behavior>
  <action>Replace `v-icon` and `v-chip` in `ClassLocation.vue` with `AppIcon`, `UiBadge`, and native anchor/span markup. Preserve provider labels/icons/colors, offline room label behavior, `size`, `inline`, `@click.stop`, target, and `rel="noopener"` behavior. Map current Vuetify color strings to `UiBadge` tone values without changing displayed labels.</action>
  <acceptance_criteria>
    - `ClassLocation.vue` has no `<v-` or `</v-` tags.
    - Inline and default modes remain present.
    - `target="_blank"` links include `rel="noopener"`.
    - Provider labels remain Google Meet, Zoom, Online, and existing offline labels.
  </acceptance_criteria>
  <verify>
    <automated>$lt=[char]60; $bad = rg -n "(($lt)style|($lt)v-|($lt)/v-|lang=[`"']scss[`"']|\.scss|@use|@forward)" client/app/components/ClassLocation.vue; if ($bad) { $bad; exit 1 }</automated>
    <automated>rg -n "UiBadge|AppIcon|target=|noopener|@click.stop|inline" client/app/components/ClassLocation.vue</automated>
    <automated>npm.cmd --prefix client run lint</automated>
    <automated>npm.cmd --prefix client run typecheck</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing page callers to proof wrappers | Public props and behavior must remain compatible. |
| External meeting links | `ClassLocation` renders links controlled by class data. |
| Global toast surface | App-wide feedback is visible to all roles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Spoofing | `ClassLocation` meeting links | mitigate | Preserve `target="_blank"` with `rel="noopener"` and do not alter displayed provider labels. |
| T-02-15 | Denial of Service | `AppSkeleton`, `AppToast` | mitigate | Preserve stable dimensions and responsive toast placement; require desktop/mobile visual parity check. |
| T-02-16 | Information Disclosure | proof wrappers | mitigate | Do not change feature composables or tenant-scoped data flow; wrappers stay display-only. |
| T-02-SC | Tampering | npm package surface | mitigate | No package install in this plan. |
</threat_model>

<verification>
- `npm.cmd --prefix client run lint`
- `npm.cmd --prefix client run typecheck`
- Static scan touched files for `<style>`, Vuetify tags, and SCSS markers.
- Human desktop/mobile visual parity check for proof surfaces per D-06: inspect `AppSkeleton`, `AppToast`, and `ClassLocation`; record no overlap, clipping, unreadable text, broken wrapping, missing states, or changed workflow in `02-06-SUMMARY.md`.
</verification>

<success_criteria>
Plan 06 is complete when proof wrappers are Tailwind-only, behavior-compatible, typecheck/lint pass, and human visual parity is recorded.
</success_criteria>

<output>
Create `.planning/phases/02-tailwind-design-system-shared-ui-kit/02-06-SUMMARY.md` when done.
</output>
