---
phase: 02-tailwind-design-system-shared-ui-kit
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - client/app/components/ui/UiPagination.vue
  - client/app/components/TablePager.vue
autonomous: true
requirements:
  - UIKIT-03
  - UIKIT-05
must_haves:
  truths:
    - "Existing `TablePager` imports continue working with the same `meta`, `page`, and `limit` public API."
    - "New `UiPagination` exposes the reusable pagination primitive for priority page redesigns."
    - "Pagination range math, page-size options, previous/next disabled states, and i18n copy remain behavior-compatible."
  prohibitions:
    - "No page call-site migration and no route/query behavior changes in this phase plan."
  artifacts:
    - path: "client/app/components/ui/UiPagination.vue"
      provides: "Reusable pagination primitive"
    - path: "client/app/components/TablePager.vue"
      provides: "Compatibility wrapper over UiPagination"
  key_links:
    - from: "client/app/components/TablePager.vue"
      to: "client/app/components/ui/UiPagination.vue"
      via: "TablePager delegates behavior and props to UiPagination"
      pattern: "UiPagination"
---

## Phase Goal

**As a** developer, **I want to** build app pages using shared Tailwind UI components, **so that** teacher and student screens stay consistent without repeated Vuetify-era markup.

<objective>
Extract the existing pagination behavior into `UiPagination` while keeping `TablePager` as a stable public wrapper.

Purpose: Satisfy the pagination slice of UIKIT-03 and prove UIKIT-05 with a low-risk shared surface migration per D-16 and D-17.
Output: `UiPagination` plus an updated `TablePager` wrapper that preserves current props/models and behavior.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-CONTEXT.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-RESEARCH.md
@client/app/components/TablePager.vue
@client/app/pages/audit-logs.vue
@client/app/pages/assistants/index.vue
@client/app/components/ui/UiSelect.vue
@client/app/components/ui/UiButton.vue
@client/app/components/AppIcon.vue
</context>

## Artifacts this phase produces

- `client/app/components/ui/UiPagination.vue`
- Updated `client/app/components/TablePager.vue`
- Components/symbols: `UiPagination`, existing `TablePager` wrapper

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UiPagination from TablePager behavior</name>
  <files>client/app/components/ui/UiPagination.vue</files>
  <read_first>
    - client/app/components/ui/UiPagination.vue if it exists
    - client/app/components/TablePager.vue
    - client/app/components/ui/UiSelect.vue
    - client/app/components/ui/UiButton.vue
    - client/app/components/AppIcon.vue
    - .planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Given `meta.total === 0`, range starts at 0 and ends at 0.
    - Given page/limit/total, range math matches current `TablePager`: `(page - 1) * limit + 1` through `min(page * limit, total)`.
    - Page-size options include the current `limit` plus 10, 20, 30, 40, and 50, sorted ascending.
    - Previous/next buttons clamp page between 1 and `max(totalPages, 1)`.
  </behavior>
  <action>Create `UiPagination` with `meta`, required `v-model:page`, required `v-model:limit`, range label support, page-size select, previous/next controls, and accessible nav labels. Preserve the exact range math and size list from `TablePager`. Use existing i18n strings through caller-provided labels or the same `useI18n` pattern only for local copy; do not call feature composables or APIs.</action>
  <acceptance_criteria>
    - `UiPagination.vue` contains the same range and size option behavior as current `TablePager.vue`.
    - Component has no `<style>`, Vuetify tags, SCSS markers, or feature data calls.
    - Buttons are disabled correctly at first/last page.
  </acceptance_criteria>
  <verify>
    <automated>$lt=[char]60; $bad = rg -n "(($lt)style|($lt)v-|($lt)/v-|lang=[`"']scss[`"']|\.scss|@use|@forward)" client/app/components/ui/UiPagination.vue; if ($bad) { $bad; exit 1 }</automated>
    <automated>rg -n "meta\\.total === 0|new Set|totalPages|update:page|update:limit|Pagination" client/app/components/ui/UiPagination.vue</automated>
    <automated>npm.cmd --prefix client run typecheck</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert TablePager to compatibility wrapper</name>
  <files>client/app/components/TablePager.vue</files>
  <read_first>
    - client/app/components/TablePager.vue
    - client/app/components/ui/UiPagination.vue
    - client/app/pages/assistants/index.vue
    - client/app/pages/audit-logs.vue
    - .planning/phases/02-tailwind-design-system-shared-ui-kit/02-CONTEXT.md
  </read_first>
  <behavior>
    - Existing call sites using `<TablePager v-model:page v-model:limit :meta>` compile unchanged.
    - `TablePager` delegates rendering and behavior to `UiPagination`.
    - `PaginationMeta` import compatibility remains intact.
  </behavior>
  <action>Update `TablePager.vue` to keep its current public props/models and delegate to `UiPagination`, preserving compatibility for all current pages. Do not migrate page call sites in this plan. Keep i18n labels identical to current rendered copy.</action>
  <acceptance_criteria>
    - Existing `TablePager` prop/model names remain `meta`, `page`, and `limit`.
    - `TablePager.vue` contains `UiPagination`.
    - `rg` confirms current page files still import/use `TablePager` unchanged where present.
  </acceptance_criteria>
  <verify>
    <automated>$lt=[char]60; $bad = rg -n "(($lt)style|($lt)v-|($lt)/v-|lang=[`"']scss[`"']|\.scss|@use|@forward)" client/app/components/TablePager.vue; if ($bad) { $bad; exit 1 }</automated>
    <automated>rg -n "UiPagination|defineModel.*page|defineModel.*limit|PaginationMeta" client/app/components/TablePager.vue</automated>
    <automated>npm.cmd --prefix client run lint</automated>
    <automated>npm.cmd --prefix client run typecheck</automated>
  </verify>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pagination metadata to UI | API-provided counts enter pagination display but page changes remain caller-owned. |
| Existing pages to wrapper | Existing pages rely on stable `TablePager` API. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Denial of Service | `UiPagination` | mitigate | Clamp page changes to valid range and preserve disabled states for prev/next controls. |
| T-02-10 | Information Disclosure | `TablePager` wrapper | mitigate | Do not alter page data fetching or tenant-scoped query composables. |
| T-02-SC | Tampering | npm package surface | mitigate | No package install in this plan. |
</threat_model>

<verification>
- `npm.cmd --prefix client run lint`
- `npm.cmd --prefix client run typecheck`
- Static scan `UiPagination.vue` and `TablePager.vue` for forbidden CSS/Vuetify markers.
</verification>

<success_criteria>
Plan 04 is complete when `UiPagination` exists and `TablePager` proves shared component adoption without breaking current callers or changing pagination behavior.
</success_criteria>

<output>
Create `.planning/phases/02-tailwind-design-system-shared-ui-kit/02-04-SUMMARY.md` when done.
</output>
