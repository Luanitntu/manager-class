---
phase: 05-responsive-qa-regression-tests-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/components/AppState.vue
  - server/package.json
  - server/scripts/smoke-student.cjs
  - server/scripts/smoke-teacher.cjs
  - .planning/phases/05-responsive-qa-regression-tests-release-readiness/05-REGRESSION.md
autonomous: true
requirements: [VER-01, VER-02, VER-03, VER-04, VER-05]
user_setup: []
must_haves:
  truths:
    - "D-01: Use existing repo commands as the primary release gate: backend lint/build/Jest and frontend lint/typecheck/build."
    - "D-02: Add lightweight repeatable smoke scripts only where they reduce manual risk without introducing a full new test stack."
    - "D-04: Existing lint warnings should be fixed when low-risk, especially warnings from shared Phase 2 components."
    - "D-08: API smoke should confirm seeded teacher/student data still exists before browser QA."
  artifacts:
    - path: "server/scripts/smoke-student.cjs"
      provides: "Repeatable authenticated student API/data smoke script"
    - path: "server/scripts/smoke-teacher.cjs"
      provides: "Repeatable authenticated teacher API/data smoke script"
    - path: ".planning/phases/05-responsive-qa-regression-tests-release-readiness/05-REGRESSION.md"
      provides: "Automated regression command results and residual warning notes"
---

<objective>
Turn the one-off Phase 4 smoke checks into repeatable regression assets, remove low-risk lint warning noise, and record a current automated verification baseline before browser QA.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-responsive-qa-regression-tests-release-readiness/05-CONTEXT.md
@.planning/phases/04-student-portal-polish-fixes/04-STUDENT-SMOKE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
@client/app/components/AppState.vue
@server/package.json
@server/prisma/seed.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove low-risk shared UI lint warnings</name>
  <files>client/app/components/AppState.vue</files>
  <read_first>client/app/components/AppState.vue, client/package.json</read_first>
  <action>Add explicit defaults for optional `title`, `body`, `icon`, and `actionLabel` in `withDefaults` so frontend lint has no `vue/require-default-prop` warnings for the shared state component. Preserve current fallback rendering behavior.</action>
  <verify>cd client; & 'C:\Program Files\nodejs\npm.cmd' run lint</verify>
  <acceptance_criteria>
    - Frontend lint no longer reports `AppState.vue` default-prop warnings.
    - Loading/empty/error/forbidden/success states still use variant defaults when props are omitted.
  </acceptance_criteria>
  <done>Shared AppState warnings are removed without changing UI behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add repeatable student smoke script</name>
  <files>server/scripts/smoke-student.cjs, server/package.json</files>
  <read_first>server/package.json, server/prisma/seed.ts, .planning/phases/04-student-portal-polish-fixes/04-STUDENT-SMOKE.md</read_first>
  <action>Create `server/scripts/smoke-student.cjs` from the successful Phase 4 API smoke pattern. It should login as the seeded student, verify `/auth/me`, `/dashboard`, `/classes`, `/documents`, `/students/me/scores`, `/students/me/comments`, `/payments/tuitions`, tuition detail receipt `SMOKE-STUDENT-001`, and confirm `/students` is forbidden. Add an npm script such as `smoke:student`.</action>
  <verify>cd server; & 'C:\Program Files\nodejs\npm.cmd' run smoke:student</verify>
  <acceptance_criteria>
    - Script exits non-zero on any failed student smoke assertion.
    - Script prints PASS/FAIL lines useful for release notes.
    - Script assumes backend is running on `http://localhost:3001/api/v1` unless overridden by env var.
  </acceptance_criteria>
  <done>Student smoke is repeatable from npm.</done>
</task>

<task type="auto">
  <name>Task 3: Add repeatable teacher smoke script</name>
  <files>server/scripts/smoke-teacher.cjs, server/package.json</files>
  <read_first>server/package.json, server/prisma/seed.ts, client/app/utils/navigation.ts</read_first>
  <action>Create `server/scripts/smoke-teacher.cjs` that logs in as the seeded teacher and verifies `/auth/me`, `/dashboard`, `/classes`, `/sessions` or the calendar API path used by `useSessions`, `/students`, `/documents`, `/payments/tuitions`, and `/profile`-equivalent auth/profile data where available. Add an npm script such as `smoke:teacher`. If a profile endpoint is not available beyond `/auth/me`, document that in the script output and regression notes.</action>
  <verify>cd server; & 'C:\Program Files\nodejs\npm.cmd' run smoke:teacher</verify>
  <acceptance_criteria>
    - Script confirms seeded teacher can read core teacher data routes.
    - Calendar/session check uses the actual existing API/composable path.
    - Any unavailable profile-specific endpoint is documented rather than treated as silently passed.
  </acceptance_criteria>
  <done>Teacher smoke is repeatable from npm.</done>
</task>

<task type="auto">
  <name>Task 4: Run and record automated regression gate</name>
  <files>.planning/phases/05-responsive-qa-regression-tests-release-readiness/05-REGRESSION.md</files>
  <read_first>client/package.json, server/package.json, .planning/phases/05-responsive-qa-regression-tests-release-readiness/05-CONTEXT.md</read_first>
  <action>Run backend targeted/full Jest as practical, backend lint/build, frontend lint/typecheck/build, `prisma:deploy`, `prisma:seed`, and both smoke scripts. Record exact commands, pass/fail status, warnings, and environment notes in `05-REGRESSION.md`. If any command cannot run, document why and the closest completed check.</action>
  <verify>rg -n "backend lint|frontend typecheck|smoke:student|smoke:teacher|PASS|FAIL" .planning/phases/05-responsive-qa-regression-tests-release-readiness/05-REGRESSION.md</verify>
  <acceptance_criteria>
    - VER-01 and VER-02 have current evidence.
    - VER-03 has smoke/test/manual evidence mapping for fixed bugs.
    - Residual warnings are explicitly listed with severity.
  </acceptance_criteria>
  <done>Automated regression baseline is recorded.</done>
</task>

</tasks>

<verification>
- From `server/`, run `npm test`, `npm run lint`, `npm run build`, `npm run prisma:deploy`, `npm run prisma:seed`, `npm run smoke:student`, and `npm run smoke:teacher` using absolute Node/npm paths if needed.
- From `client/`, run `npm run lint`, `npm run typecheck`, and `npm run build` using absolute Node/npm paths if needed.
- Confirm `05-REGRESSION.md` contains command results and residual warning notes.
</verification>

<success_criteria>
- Low-risk lint warnings are removed or documented.
- Student and teacher smoke checks are repeatable.
- Automated regression evidence is current and ready for browser QA.
</success_criteria>

<output>
Create `.planning/phases/05-responsive-qa-regression-tests-release-readiness/05-01-SUMMARY.md` after this plan completes.
</output>
