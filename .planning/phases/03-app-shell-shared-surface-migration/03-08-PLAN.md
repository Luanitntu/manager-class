---
phase: 03-app-shell-shared-surface-migration
plan: 08
type: gap_closure
wave: 1
depends_on:
  - 03-01-PLAN.md
files_modified:
  - client/app/components/login/LoginFormPane.vue
autonomous: true
requirements:
  - APP-05
must_haves:
  truths:
    - "Login social auth buttons keep disabled behavior while preserving the pre-migration social button and icon dimensions."
    - "The fix uses active Tailwind/template sizing, not orphaned Vuetify-era CSS selectors."
  artifacts:
    - path: "client/app/components/login/LoginFormPane.vue"
      provides: "fixed social button height and icon dimensions"
---

# Phase 03 Gap Plan 08: Restore Login Social Button Dimensions

<objective>
Close the Phase 3 UAT cosmetic gap where the disabled Google and GitHub login buttons, plus their icons, changed size after the auth Tailwind migration.
</objective>

<context>
@.planning/phases/03-app-shell-shared-surface-migration/03-UAT.md
@client/app/components/login/LoginFormPane.vue
@client/app/components/ui/UiButton.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restore login social button and icon sizing</name>
  <files>client/app/components/login/LoginFormPane.vue</files>
  <action>
  Update the two disabled social UiButton instances in the login form so they preserve the old visual dimensions:
  - Add active Tailwind sizing classes to both social buttons for 42px height/min-height, 14px text, and stable center alignment.
  - Replace the GitHub leadingIcon prop with an explicit leading slot that renders AppIcon size 20.
  - Give the custom Google SVG explicit active sizing classes such as h-5 w-5 shrink-0.
  - Preserve disabled state, titles, labels, grid behavior, and auth flow behavior.
  - Do not re-import login.css or reintroduce Vuetify/SCSS coupling.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-))" client/app/components/login/LoginFormPane.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
    <manual>Inspect /login at desktop and mobile widths: Google and GitHub disabled buttons are the same height, icons are visually 20px, labels remain centered, and no overflow/overlap appears.</manual>
  </verify>
  <done>Social login buttons match the intended pre-migration dimensions without restoring old UI dependencies.</done>
</task>

</tasks>

<risks>
- Changing UiButton globally could affect unrelated pages, so keep the fix local to LoginFormPane.
- The old CSS file is not part of the active login page; do not rely on .login-social-icon or .login-social-button.v-btn selectors.
</risks>

<success_criteria>
The UAT-reported cosmetic gap is closed by local template/Tailwind sizing, with auth behavior unchanged and targeted old-marker scan passing.
</success_criteria>
