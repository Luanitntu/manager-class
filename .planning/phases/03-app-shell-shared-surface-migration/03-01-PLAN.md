---
phase: 03-app-shell-shared-surface-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/layouts/default.vue
  - client/app/layouts/auth.vue
  - client/app/components/AuthShell.vue
  - client/app/components/login/LoginFormPane.vue
  - client/app/components/login/LoginVisual.vue
  - client/app/components/register/RegisterFormPane.vue
  - client/app/components/register/RegisterVisual.vue
  - client/app/pages/forgot-password.vue
  - client/app/pages/reset-password.vue
  - client/app/pages/verify-email.vue
autonomous: true
requirements:
  - APP-01
  - APP-05
must_haves:
  truths:
    - "D-01, D-10: auth and shared shell surfaces prioritize teacher/student/auth workflows without expanding into admin-heavy pages."
    - "D-11, D-12: auth pages keep current validation, loading, error, remember-me, redirect, and disabled social-auth behavior while Ui* primitives only display external state."
    - "D-13, D-14, D-15: teacher, student, and assistant shell branches plus auth pages have desktop and mobile visual QA notes or screenshots."
  artifacts:
    - path: "client/app/layouts/default.vue"
      provides: "role-aware Tailwind shell preservation check"
    - path: "client/app/layouts/auth.vue"
      provides: "auth layout wrapper"
    - path: "client/app/components/AuthShell.vue"
      provides: "Tailwind auth brand shell"
    - path: "client/app/components/login/LoginFormPane.vue"
      provides: "login form migration"
    - path: "client/app/components/register/RegisterFormPane.vue"
      provides: "register form migration"
    - path: "client/app/pages/forgot-password.vue"
      provides: "password recovery form migration"
    - path: "client/app/pages/reset-password.vue"
      provides: "password reset form migration"
    - path: "client/app/pages/verify-email.vue"
      provides: "email verification state migration"
  key_links:
    - from: "auth form pages/components"
      to: "existing auth composables, VeeValidate, and Zod state"
      via: "form handlers remain page/component-owned"
    - from: "default shell"
      to: "role-aware navigation"
      via: "existing auth store and Nuxt routes remain unchanged"
---

<objective>
Migrate the app shell preservation checks and full auth surface away from old UI primitives while preserving role-aware navigation and all auth flows.

Purpose: satisfy APP-01 for shared layout/auth blockers and establish APP-05 visual parity evidence early.
Output: Tailwind-only auth shell/pages using Phase 2 Ui* primitives, with shell branch behavior verified for teacher/student/assistant.
</objective>

<execution_context>
@D:/Code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@D:/Code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-app-shell-shared-surface-migration/03-CONTEXT.md
@.planning/phases/03-app-shell-shared-surface-migration/03-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-RESEARCH.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
@client/app/components/ui/README.md
@client/app/layouts/default.vue
@client/app/layouts/auth.vue
@client/app/components/AuthShell.vue
@client/app/components/login/LoginFormPane.vue
@client/app/components/login/LoginVisual.vue
@client/app/components/register/RegisterFormPane.vue
@client/app/components/register/RegisterVisual.vue
@client/app/pages/forgot-password.vue
@client/app/pages/reset-password.vue
@client/app/pages/verify-email.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Preserve role-aware shell and migrate auth wrapper</name>
  <files>client/app/layouts/default.vue, client/app/layouts/auth.vue, client/app/components/AuthShell.vue</files>
  <action>Keep existing default shell route groups, auth store usage, logout, language switcher, announcement, mobile drawer, profile menu, outside-click, route-change, and Escape close behavior. Verify teacher calendar remains visible in teacher daily navigation per D-01 and D-13. Convert AuthShell brand/avatar/icon and any old component/style coupling to Tailwind plus AppIcon, using 36px or 40px tokenized brand square per D-10 and D-11. Do not alter route middleware or auth token/session handling.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-(app|main|navigation-drawer|app-bar|list)|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/layouts/default.vue client/app/layouts/auth.vue client/app/components/AuthShell.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
  </verify>
  <done>Default/auth shell contains no old shell primitives or component CSS markers; teacher/student/assistant nav branches and auth wrapper still render with same route behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate login and register surfaces</name>
  <files>client/app/components/login/LoginFormPane.vue, client/app/components/login/LoginVisual.vue, client/app/components/register/RegisterFormPane.vue, client/app/components/register/RegisterVisual.vue</files>
  <action>Replace old form, button, checkbox, icon, avatar, divider, sheet, and image primitives with UiInput, UiCheckbox, UiButton, UiAlert where needed, AppIcon, native img, and Tailwind layout. Preserve VeeValidate/Zod bindings, validation messages, rememberMe, redirects, loading/disabled state, social button disabled behavior, existing imagery, and current copy per D-11 and D-12. Remove imported form/visual CSS only after equivalent Tailwind layout classes preserve the split-panel auth contract.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/login/LoginFormPane.vue client/app/components/login/LoginVisual.vue client/app/components/register/RegisterFormPane.vue client/app/components/register/RegisterVisual.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run typecheck"</automated>
  </verify>
  <done>Login/register forms submit through existing handlers, show validation/errors/loading, preserve unavailable social buttons, and are Tailwind-only with no old UI markers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate password recovery and email verification pages</name>
  <files>client/app/pages/forgot-password.vue, client/app/pages/reset-password.vue, client/app/pages/verify-email.vue</files>
  <action>Convert recovery/reset/verify pages to AuthShell plus UiInput, UiButton, UiAlert, UiSpinner, and AppIcon. Preserve submit handlers, token/query behavior, success/error/verifying states, disabled/loading guards, and navigation links per D-10 through D-12. Verification status must expose semantic status text and icon, not color-only signaling.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/pages/forgot-password.vue client/app/pages/reset-password.vue client/app/pages/verify-email.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
  </verify>
  <done>Forgot/reset/verify pages keep current auth behavior and visible states, pass targeted marker scan, and record desktop/mobile auth visual QA evidence.</done>
</task>

</tasks>

<risks>
- Auth regressions can block all roles; avoid changing auth store, token handling, route middleware, or backend contracts.
- Native UiSelect/UiInput components display state only; validation ownership stays in the existing page/form code per D-12.
- Shell edits can hide teacher calendar access; verify teacher branch before closing APP-01/APP-05 for this plan.
- Scope rationale for checker warning: this plan intentionally keeps the 10 shell/auth files together because D-10 requires the whole auth surface in Phase 3 and the files share one AuthShell/form/visual contract. Splitting would create dependency churn across the same auth UX without reducing implementation risk.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser auth form to API | Public credential/reset inputs cross into existing auth endpoints through existing handlers. |
| persisted auth store to shell | Role/session state controls visible navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Spoofing | auth forms | mitigate | Preserve existing VeeValidate/Zod validation and submit handlers; do not alter auth API calls or token storage. |
| T-03-01-02 | Elevation of privilege | default shell navigation | mitigate | Preserve existing role branch conditions and route links; UI-only migration must not create new access assumptions. |
| T-03-01-SC | Tampering | package installs | accept | No package install is planned; do not add new UI dependencies. |
</threat_model>

<verification>
Run targeted static scans after each task. Run `npm.cmd --prefix client run lint` and `npm.cmd --prefix client run typecheck` with the nvm path if npm is unavailable on PATH. Capture manual or screenshot QA notes for teacher/student/assistant shell branches plus login/register/recovery/reset/verify at desktop and mobile widths per D-13 through D-15.
</verification>

<success_criteria>
APP-01 shell/auth layout blockers are migrated or verified Tailwind-only, APP-05 visual parity evidence exists for touched auth/shell surfaces, and no backend files are changed.
</success_criteria>

<output>
Create `.planning/phases/03-app-shell-shared-surface-migration/03-01-SUMMARY.md` when done.
</output>
