---
phase: 03-app-shell-shared-surface-migration
plan: 09
type: gap_closure
wave: 1
depends_on:
  - 03-08-PLAN.md
files_modified:
  - client/app/components/login/LoginFormPane.vue
autonomous: true
requirements:
  - APP-05
must_haves:
  truths:
    - "The Google social login button renders its icon and label on one row at desktop and mobile widths."
    - "The fix keeps Google and GitHub social buttons at the restored 42px height with 20px icons."
  artifacts:
    - path: "client/app/components/login/LoginFormPane.vue"
      provides: "Google social icon moved to UiButton leading slot and social labels prevented from wrapping"
---

# Phase 03 Gap Plan 09: Keep Google Social Login Content Inline

<objective>
Close the residual Phase 3 UAT cosmetic gap where the Google social login icon and text still wrapped onto two rows after Plan 08.
</objective>

<context>
@.planning/phases/03-app-shell-shared-surface-migration/03-UAT.md
@client/app/components/login/LoginFormPane.vue
@client/app/components/ui/UiButton.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Prevent Google social button wrapping</name>
  <files>client/app/components/login/LoginFormPane.vue</files>
  <action>
  Update the Google social UiButton so its icon and text are separate flex items:
  - Move the Google SVG into a `#leading` slot, matching the GitHub pattern.
  - Keep the Google SVG at explicit `h-5 w-5 shrink-0` sizing.
  - Add `whitespace-nowrap` to both social buttons so labels cannot wrap.
  - Preserve disabled state, labels, titles, grid behavior, and auth flow behavior.
  - Do not change shared UiButton globally.
  </action>
  <verify>
    <automated>rg -n '(&lt;)/?v-[a-z]|(&lt;)style|lang="scss"|\.scss|:deep\(\.v-' client/app/components/login/LoginFormPane.vue exits with no matches</automated>
    <automated>npm.cmd --prefix client run lint</automated>
    <automated>npm.cmd --prefix client run typecheck</automated>
    <automated>npm.cmd --prefix client run build</automated>
    <manual>Inspect /login: Google icon and text render on one row in the 42px button.</manual>
  </verify>
  <done>Google social login button content stays inline without changing shared button behavior.</done>
</task>

</tasks>

<risks>
- Changing UiButton globally could affect unrelated pages, so keep the fix local to LoginFormPane.
- A default-slot icon is wrapped inside UiButton's label span; keep custom social icons in leading slots.
</risks>

<success_criteria>
The residual Google social button wrapping gap is closed by local template/Tailwind sizing, with frontend verification passing.
</success_criteria>
