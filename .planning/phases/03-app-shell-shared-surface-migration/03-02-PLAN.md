---
phase: 03-app-shell-shared-surface-migration
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - client/app/components/calendar/TeacherCalendar.vue
  - client/app/components/calendar/TeacherCalendarBoard.vue
  - client/app/components/calendar/TeacherCalendarHeader.vue
  - client/app/components/calendar/TeacherSessionDetail.vue
  - client/app/components/calendar/StudentCalendar.vue
autonomous: true
requirements:
  - APP-02
  - APP-04
  - APP-05
must_haves:
  truths:
    - "D-07, D-09: teacher calendar remains the primary workflow anchor with same hierarchy, actions, month/week behavior, cell create, drag/drop, detail panel, and feedback."
    - "D-03: touched calendar files are scan-clean for <v-*>, SCSS imports, component style blocks, and old style-coupling markers."
    - "D-14, D-15: calendar desktop/mobile QA notes or screenshots cover loading, populated, create, edit-open, drag/drop feedback, and error/success toast paths."
  artifacts:
    - path: "client/app/components/calendar/TeacherCalendar.vue"
      provides: "calendar orchestrator and toast feedback"
    - path: "client/app/components/calendar/TeacherCalendarHeader.vue"
      provides: "calendar page header actions"
    - path: "client/app/components/calendar/TeacherCalendarBoard.vue"
      provides: "interactive month/week grid"
    - path: "client/app/components/calendar/TeacherSessionDetail.vue"
      provides: "active session detail/empty panel"
    - path: "client/app/components/calendar/StudentCalendar.vue"
      provides: "student calendar toast migration"
  key_links:
    - from: "TeacherCalendarBoard.vue"
      to: "TeacherCalendar.vue"
      via: "existing emits for view/date/create/open/drop"
    - from: "TeacherCalendar.vue"
      to: "AppToast"
      via: "existing useToast/AppToast pattern instead of snackbar"
---

<objective>
Migrate the teacher calendar shell surfaces and calendar feedback to Tailwind/UI-kit components while preserving calendar-first workflow behavior.

Purpose: satisfy APP-04 and reduce high-traffic APP-02 blockers before migrating SessionDialog.
Output: Tailwind-only calendar header, board, detail, and toast surfaces with keyboard/drag/drop behavior preserved.
</objective>

<execution_context>
@D:/Code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@D:/Code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-app-shell-shared-surface-migration/03-CONTEXT.md
@.planning/phases/03-app-shell-shared-surface-migration/03-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-RESEARCH.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@client/app/components/ui/README.md
@client/app/components/calendar/TeacherCalendar.vue
@client/app/components/calendar/TeacherCalendarBoard.vue
@client/app/components/calendar/TeacherCalendarHeader.vue
@client/app/components/calendar/TeacherSessionDetail.vue
@client/app/components/calendar/StudentCalendar.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate calendar header and orchestration feedback</name>
  <files>client/app/components/calendar/TeacherCalendar.vue, client/app/components/calendar/TeacherCalendarHeader.vue, client/app/components/calendar/StudentCalendar.vue</files>
  <action>Convert header actions to UiPageHeader or UiToolbar plus UiButton/UiIconButton and AppIcon while preserving open/refresh emits and visible teacher calendar entry per D-07. Replace calendar snackbar feedback in teacher and student calendar orchestrators with existing AppToast/useToast behavior from Phase 2; keep success/error message timing and saved reload behavior. Remove calendar page/header CSS imports only after translating layout and spacing into Tailwind classes.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/calendar/TeacherCalendar.vue client/app/components/calendar/TeacherCalendarHeader.vue client/app/components/calendar/StudentCalendar.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
  </verify>
  <done>Header actions and calendar feedback use Tailwind/AppToast, retain emits and reload/error behavior, and targeted files are marker-clean.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate interactive teacher calendar board</name>
  <files>client/app/components/calendar/TeacherCalendarBoard.vue</files>
  <action>Replace old card, toolbar, buttons, and toggle primitives with UiCard, UiToolbar, UiButton, UiIconButton, UiSegmentedControl, AppIcon, and Tailwind grid/flex. Preserve month/week mode model, today/prev/next emits, event open emits, cell create emits, keyboard create on Enter/Space, drag/drop handlers when canEdit is true, today marker, drop target state, cancellation styling, stable event row height, and +N sessions overflow indicator per D-07 and D-09. Do not redesign the calendar grid beyond tokenized parity.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/calendar/TeacherCalendarBoard.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run typecheck"</automated>
  </verify>
  <done>Calendar board remains keyboard and drag/drop usable in month/week views, does not clip weekday/cell/event content on mobile, and has no old UI markers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Migrate teacher session detail panel</name>
  <files>client/app/components/calendar/TeacherSessionDetail.vue</files>
  <action>Replace old sheet/spacer/button/icon primitives with UiCard, UiEmptyState, UiBadge, UiButton, AppIcon, and Tailwind. Preserve active/empty states, active marker colored by session class, edit/open emit, canEdit guard, and readable metadata. Remove imported detail CSS after Tailwind parity is in place per D-09.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/calendar/TeacherSessionDetail.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
  </verify>
  <done>Session detail panel preserves active and empty flows, can open/edit sessions when allowed, and is Tailwind-only.</done>
</task>

</tasks>

<risks>
- `client/app/components/calendar/TeacherCalendar.vue` was dirty before planning; read current content and preserve user edits before changing it.
- Calendar CSS imports encode dense layout; translate each layout role before removing imports.
- Drag/drop and keyboard paths can be lost during template replacement; verify event handlers explicitly.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| calendar UI to session composables | User calendar actions trigger existing session mutations and reloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | drag/drop reschedule | mitigate | Preserve canEdit guards and existing drop handlers; do not alter backend/API contracts. |
| T-03-02-02 | Denial of service | calendar rendering | mitigate | Preserve loading skeleton and stable dimensions to avoid broken large-month rendering. |
| T-03-02-SC | Tampering | package installs | accept | No package install is planned. |
</threat_model>

<verification>
Targeted scans for each touched calendar file, frontend lint/typecheck, and manual/screenshot QA for desktop and mobile month/week views, loading state, populated state, create-from-cell, open existing session, drag/drop feedback, and toast error/success paths per D-14 and D-15.
</verification>

<success_criteria>
APP-04 teacher calendar workflow remains accessible and visually consistent; APP-02 calendar shared surfaces no longer depend on old UI primitives; APP-05 visual parity evidence exists for calendar surfaces.
</success_criteria>

<output>
Create `.planning/phases/03-app-shell-shared-surface-migration/03-02-SUMMARY.md` when done.
</output>
