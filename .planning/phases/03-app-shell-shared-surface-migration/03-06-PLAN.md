---
phase: 03-app-shell-shared-surface-migration
plan: 06
type: execute
wave: 6
depends_on:
  - 03-05
files_modified:
  - client/app/components/AssistantDetailDialog.vue
  - client/app/components/StudentDetailDialog.vue
autonomous: true
requirements:
  - APP-02
  - APP-03
  - APP-05
must_haves:
  truths:
    - "D-06: StudentDetailDialog and AssistantDetailDialog migrate low-risk header/avatar/cards/tables/chips/forms where behavior-compatible; exact out-of-scope markers are inventoried if a bounded area remains."
    - "D-03: shared detail dialog migration either removes old markers from touched scope or documents exact remaining old UI markers for Phase 5/follow-up cleanup."
    - "D-14, D-15: migrated dialog areas have desktop/mobile visual QA notes including overflow/internal scroll."
  artifacts:
    - path: "client/app/components/AssistantDetailDialog.vue"
      provides: "assistant detail low-risk dialog migration"
    - path: "client/app/components/StudentDetailDialog.vue"
      provides: "student detail low-risk dialog migration or exact remaining inventory"
  key_links:
    - from: "detail dialogs"
      to: "existing mutations and props/emits"
      via: "public contract remains unchanged"
---

<objective>
Migrate low-risk shared detail dialog portions, prioritizing AssistantDetailDialog and bounded StudentDetailDialog areas, while documenting any exact remaining out-of-scope old markers.

Purpose: reduce APP-02 shared dialog blockers without forcing broad rewrites beyond D-06; support APP-03 inventory accuracy.
Output: Tailwind/UI-kit dialog areas plus exact remaining old-marker notes where bounded migration is not safe within Phase 3 scope.
</objective>

<execution_context>
@D:/Code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@D:/Code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-app-shell-shared-surface-migration/03-CONTEXT.md
@.planning/phases/03-app-shell-shared-surface-migration/03-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-RESEARCH.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@client/app/components/ui/README.md
@client/app/components/AssistantDetailDialog.vue
@client/app/components/StudentDetailDialog.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate AssistantDetailDialog low-risk areas</name>
  <files>client/app/components/AssistantDetailDialog.vue</files>
  <action>Replace old dialog/card/header/avatar/close, salary config form, salary summary, salary table, assigned class chips, separators, skeleton/empty areas with UiDialog size lg or 640px equivalent, UiAvatar, UiIconButton, UiInput, UiSelect, UiButton, UiMetricCard, UiTable, UiBadge, UiSkeleton, UiEmptyState, and Tailwind. Preserve modelValue/update contract, assistant prop behavior, salary type/rate state, updateSalary loading, saveSalary mutation, assigned class data, salary history rows, and label `Save salary settings` per D-06 and UI spec. Do not alter assistant APIs.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/AssistantDetailDialog.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run typecheck"</automated>
  </verify>
  <done>AssistantDetailDialog low-risk areas are Tailwind/UI-kit based, behavior-compatible, and marker-clean unless a precise out-of-scope note is recorded in the summary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate bounded StudentDetailDialog areas or inventory exact remainder</name>
  <files>client/app/components/StudentDetailDialog.vue</files>
  <action>Attempt bounded migration of header/avatar/close, tabs, profile fields, score form controls, score list, comments entry, empty states, and comments list using UiDialog size xl or 720px equivalent, UiAvatar, UiIconButton, UiTabs, UiInput, UiTextarea, UiSelect, UiList, UiButton, UiEmptyState, AppIcon, and Tailwind. Preserve modelValue/update contract, student prop behavior, tab state, profile save, score create/delete, comment create, loading guards, ordered comments, and all existing mutation calls. If score delete UI is migrated, add UiConfirmDialog with Delete score destructive confirmation; if a sub-area remains out of scope under D-06, record exact old marker lines and the reason as context cost or dependency boundary in the summary and final inventory.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/StudentDetailDialog.vue; exit 0'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
  </verify>
  <done>StudentDetailDialog migrated bounded areas preserve profile/scores/comments behavior; any remaining old markers are exact-line inventoried for APP-03 rather than hidden.</done>
</task>

</tasks>

<risks>
- StudentDetailDialog has multiple mutation surfaces; preserve logic and avoid a broad data-flow rewrite.
- D-06 permits bounded migration; exact inventory is required for any remaining old markers.
- Score delete must not remain an unconfirmed destructive action if that area is migrated.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detail dialog forms to student/assistant APIs | Profile, score, comment, and salary edits cross into existing mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | salary/profile/score/comment forms | mitigate | Preserve existing mutations and loading guards; do not change request shapes. |
| T-03-06-02 | Tampering | score delete | mitigate | Use UiConfirmDialog if score delete UI is migrated; otherwise inventory exact remaining old behavior. |
| T-03-06-SC | Tampering | package installs | accept | No package install is planned. |
</threat_model>

<verification>
Run targeted scans, client lint/typecheck, and visual QA for assistant dialog salary/class areas and migrated student dialog tabs/forms/lists at desktop and mobile widths. Record exact remaining marker inventory if any.
</verification>

<success_criteria>
APP-02 shared detail dialog blockers are reduced safely, APP-03 has exact remaining old-marker inventory where applicable, and APP-05 visual evidence exists for migrated dialog areas.
</success_criteria>

<output>
Create `.planning/phases/03-app-shell-shared-surface-migration/03-06-SUMMARY.md` when done.
</output>
