---
phase: 03-app-shell-shared-surface-migration
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - client/app/components/dashboard/StudentWorkspaceDashboard.vue
  - client/app/components/StudentSchedule.vue
autonomous: true
requirements:
  - APP-02
  - APP-05
must_haves:
  truths:
    - "D-08: student dashboard and schedule migrate after teacher calendar and teacher dashboard."
    - "D-01: student surfaces are in core Phase 3 scope; admin-heavy and Phase 4 pages remain out of scope."
    - "D-03, D-14, D-15: student dashboard/schedule are marker-clean and visually checked at desktop and mobile widths."
  artifacts:
    - path: "client/app/components/dashboard/StudentWorkspaceDashboard.vue"
      provides: "student dashboard shared surface"
    - path: "client/app/components/StudentSchedule.vue"
      provides: "student weekly schedule shared surface"
  key_links:
    - from: "StudentSchedule.vue"
      to: "parent week navigation"
      via: "existing emits and props remain unchanged"
---

<objective>
Migrate student dashboard and weekly schedule shared surfaces to Tailwind/UI-kit components while preserving loading, empty, populated, progress, and action states.

Purpose: complete teacher/student core shared-surface migration for APP-02 and APP-05 after teacher-first flows.
Output: Tailwind-only student dashboard and schedule surfaces with responsive stacking and no old UI markers.
</objective>

<execution_context>
@D:/Code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@D:/Code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-app-shell-shared-surface-migration/03-CONTEXT.md
@.planning/phases/03-app-shell-shared-surface-migration/03-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-RESEARCH.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@client/app/components/ui/README.md
@client/app/components/dashboard/StudentWorkspaceDashboard.vue
@client/app/components/StudentSchedule.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate student dashboard</name>
  <files>client/app/components/dashboard/StudentWorkspaceDashboard.vue</files>
  <action>Replace old icons, buttons, progress, cards, scoped CSS layout, and style coupling with UiPage or section layout, UiCard, UiMetricCard, UiBadge, UiProgress, UiList, UiEmptyState, UiButton, UiIconButton, AppIcon, and Tailwind grids. Preserve next-session hero with real image, stat cards, urgent upcoming sessions, course progress, tasks, study stats, loading skeleton behavior, existing routes, and action labels. Mobile layout must stack without clipping or horizontal scroll per D-14.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/dashboard/StudentWorkspaceDashboard.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run typecheck"</automated>
  </verify>
  <done>Student dashboard keeps current data/states/routes, uses UiProgress for course progress, stacks safely on mobile, and has no old markers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate student weekly schedule</name>
  <files>client/app/components/StudentSchedule.vue</files>
  <action>Replace week navigation buttons, empty icon, session action button, old icon usage, scoped CSS, and icon deep coupling with UiButton, UiIconButton, UiEmptyState, UiBadge, UiCard or Tailwind session rows, AppIcon, and Tailwind responsive layout. Preserve previous/title/next/today event contract from parent, loading list skeleton, empty weekly schedule, grouping by day, online/offline labels, action labels, and existing props/emits. Timeline keeps left date rail on desktop and stacks safely on narrow mobile per D-14.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/StudentSchedule.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
  </verify>
  <done>Student schedule keeps week navigation, loading/empty/populated states, day grouping, session cards, and action behavior while becoming marker-clean.</done>
</task>

</tasks>

<risks>
- Student dashboard scoped CSS controls responsive behavior; preserve breakpoints with explicit Tailwind grids.
- Schedule timeline can clip on mobile; verify stacking and long class/location names.
- Student surfaces should not gain API calls or tenant logic in UI primitives.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| student session/dashboard data to UI | Existing student data renders schedule/progress/action surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information disclosure | student dashboard/schedule | mitigate | Preserve current data ownership and do not add direct API calls in components beyond existing patterns. |
| T-03-05-02 | Denial of service | responsive schedule | mitigate | Use stable card dimensions and mobile stacking; verify no horizontal scroll. |
| T-03-05-SC | Tampering | package installs | accept | No package install is planned. |
</threat_model>

<verification>
Run targeted scans, client lint/typecheck, and visual QA for student dashboard loading/populated states plus schedule loading/populated/empty week at desktop and mobile widths.
</verification>

<success_criteria>
Student dashboard and schedule satisfy APP-02 and APP-05 for student shared surfaces with preserved behavior, marker-clean files, and recorded visual QA evidence.
</success_criteria>

<output>
Create `.planning/phases/03-app-shell-shared-surface-migration/03-05-SUMMARY.md` when done.
</output>
