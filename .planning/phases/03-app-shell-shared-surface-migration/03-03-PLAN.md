---
phase: 03-app-shell-shared-surface-migration
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - client/app/components/SessionDialog.vue
autonomous: true
requirements:
  - APP-02
  - APP-04
  - APP-05
must_haves:
  truths:
    - "D-04, D-05: SessionDialog is migrated with stronger QA and scheduling correctness before visual polish."
    - "D-05: class/student/assistant selection, start/end time, recurrence, conflict-facing errors, create/edit, saved reload, delete/status actions, and existing contracts are preserved."
    - "D-14, D-15: SessionDialog visual QA includes create single, create recurring, edit, delete confirmation, mark completed/reopen, desktop, and mobile-width checks."
  artifacts:
    - path: "client/app/components/SessionDialog.vue"
      provides: "calendar-critical session create/edit dialog"
      exports:
        - "modelValue prop"
        - "session prop"
        - "prefill prop"
        - "saved emit"
        - "update:modelValue emit"
  key_links:
    - from: "SessionDialog.vue"
      to: "session composables"
      via: "existing create/update/bulk/delete/status mutations"
    - from: "SessionDialog.vue"
      to: "TeacherCalendar.vue"
      via: "saved emit triggers calendar reload"
---

<objective>
Fully migrate the calendar-critical SessionDialog to Tailwind/UI-kit primitives while preserving all scheduling contracts and mutation behavior.

Purpose: satisfy the highest-risk APP-02 blocker and protect APP-04 teacher calendar correctness.
Output: Tailwind-only SessionDialog using UiDialog, UiConfirmDialog, UiInput, UiSelect, UiSegmentedControl, UiAlert, UiBadge, UiButton, and UiActionGroup.
</objective>

<execution_context>
@D:/Code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@D:/Code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-app-shell-shared-surface-migration/03-CONTEXT.md
@.planning/phases/03-app-shell-shared-surface-migration/03-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-RESEARCH.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@client/app/components/ui/README.md
@client/app/components/SessionDialog.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace dialog shell and primary form controls</name>
  <files>client/app/components/SessionDialog.vue</files>
  <action>Replace old dialog/card/title/text/form primitives with UiDialog size md, UiAlert, UiSegmentedControl for single/recurring mode, UiInput for date/time/topic fields, UiSelect for class and instructor fields, UiBadge for status/location/day indicators, and AppIcon where needed. Preserve props and emits exactly: modelValue, session, prefill, saved, update:modelValue. Map class and instructor options to UiSelect item shape before binding; do not pass raw Vuetify item-title/item-value assumptions. Preserve useUserTimezone, extractApiError, auth store, current form initialization, and create/edit mode switching per D-04 and D-05.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run typecheck"</automated>
  </verify>
  <done>Dialog opens/closes through the same model contract; single/recurring controls, class/instructor selects, date/time/topic fields, and error display bind to existing form state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Preserve recurrence, status, delete, and save actions</name>
  <files>client/app/components/SessionDialog.vue</files>
  <action>Replace weekday chip group with caller-owned Tailwind button/checkbox group that preserves multiple day selection. Replace actions with UiActionGroup and UiButton. Add UiConfirmDialog for Delete session with destructive copy from the UI spec before mutation. Preserve save, update, bulk create, delete, mark completed, reopen, disabled/loading guards, error extraction, and saved emit. Footer must place destructive/status actions left and cancel/save actions right on desktop, stacking safely on mobile per D-05.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/SessionDialog.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
  </verify>
  <done>SessionDialog has no old UI markers; create/update/bulk/delete/status mutations retain existing loading, disabled, error, and saved behavior; delete requires explicit confirmation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record strong dialog QA evidence</name>
  <files>client/app/components/SessionDialog.vue</files>
  <action>Perform and record manual or screenshot QA for create single, create recurring with weekdays/date range, edit existing session, delete confirmation/cancel/confirm, mark completed/reopen, missing required class/date/time disabled state, API error state, desktop width, and mobile-width internal scroll per D-14 and D-15. Backend remains unchanged unless a real frontend/backend contract bug is discovered; if backend is touched, stop and document why before expanding verification.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run typecheck"</automated>
  </verify>
  <done>QA evidence covers all SessionDialog risk paths named in D-04/D-05/D-14/D-15; no backend files are changed.</done>
</task>

</tasks>

<risks>
- SessionDialog owns scheduling-critical mutation paths; preserve script logic unless strictly needed for UI binding.
- UiSelect item shape differs from Vuetify; map items explicitly.
- Delete/session status actions require confirmation and loading guards to avoid accidental destructive updates.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dialog form to session API | User form state triggers session create, update, bulk create, delete, and status mutations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | SessionDialog save/delete/status actions | mitigate | Preserve existing mutation functions, loading guards, and explicit delete confirmation. |
| T-03-03-02 | Information disclosure | class/instructor selects | mitigate | Do not alter data sources or tenant-scoped composables; UI migration only maps display items. |
| T-03-03-SC | Tampering | package installs | accept | No package install is planned. |
</threat_model>

<verification>
Targeted marker scan, client lint/typecheck, and strong manual/screenshot QA for all create/edit/recurrence/delete/status paths. Include exact notes in the summary.
</verification>

<success_criteria>
APP-02 high-risk dialog blocker is migrated; APP-04 calendar create/edit flow remains correct; APP-05 dialog desktop/mobile parity evidence exists.
</success_criteria>

<output>
Create `.planning/phases/03-app-shell-shared-surface-migration/03-03-SUMMARY.md` when done.
</output>
