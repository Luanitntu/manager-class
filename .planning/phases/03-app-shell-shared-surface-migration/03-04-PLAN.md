---
phase: 03-app-shell-shared-surface-migration
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - client/app/components/dashboard/TeacherWorkspaceDashboard.vue
autonomous: true
requirements:
  - APP-02
  - APP-04
  - APP-05
must_haves:
  truths:
    - "D-08: teacher dashboard migration follows teacher calendar and SessionDialog before student dashboard work."
    - "D-07, D-09: next-session hero and calendar CTA keep the calendar-first teacher workflow prominent without a major redesign."
    - "D-03, D-14, D-15: teacher dashboard is marker-clean and has desktop/mobile visual QA for next-session and empty states."
  artifacts:
    - path: "client/app/components/dashboard/TeacherWorkspaceDashboard.vue"
      provides: "teacher dashboard shared/high-traffic surface"
  key_links:
    - from: "TeacherWorkspaceDashboard.vue"
      to: "calendar route"
      via: "existing /calendar CTA/link remains visible"
---

<objective>
Migrate the teacher dashboard shared surface to the Phase 2 UI kit while preserving the next-session hero, metrics, calendar CTA, lists, mini calendar, actions, and empty state.

Purpose: satisfy APP-02/APP-04 for high-traffic teacher dashboard after calendar-critical surfaces.
Output: Tailwind-only teacher dashboard using UiPage/UiSection/UiCard/UiMetricCard/UiList/UiBadge/UiEmptyState/UiButton/AppIcon.
</objective>

<execution_context>
@D:/Code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@D:/Code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-app-shell-shared-surface-migration/03-CONTEXT.md
@.planning/phases/03-app-shell-shared-surface-migration/03-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-RESEARCH.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@client/app/components/ui/README.md
@client/app/components/dashboard/TeacherWorkspaceDashboard.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate next-session hero and metric cards</name>
  <files>client/app/components/dashboard/TeacherWorkspaceDashboard.vue</files>
  <action>Replace old hero sheet/container/chip/button/icon/card/stat grid primitives with Tailwind bands plus UiCard or unframed sections, UiMetricCard, UiBadge, UiButton, and AppIcon. Preserve next-session present and no-next-session flows, all existing computed values, route targets, labels, and calendar CTA prominence per D-07 through D-09. Use 8px radius surfaces and avoid nested card styling.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run typecheck"</automated>
  </verify>
  <done>Teacher dashboard hero and metrics render with the same data and route behavior, preserve calendar-first CTA, and use Tailwind/UI-kit surfaces.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate upcoming sessions, mini calendar, and action panels</name>
  <files>client/app/components/dashboard/TeacherWorkspaceDashboard.vue</files>
  <action>Replace old row/col/list/card/title/chip/icon/menu primitives and imported dashboard CSS with responsive Tailwind grids, UiList/UiListItem, UiBadge, UiEmptyState, UiIconButton or UiButton, and AppIcon. Preserve upcoming sessions list, class/link/tags/action affordance, mini month calendar layout, urgent action items, and empty upcoming-session state. Mobile must stack to one column without overlap by tablet/narrow widths per D-14.</action>
  <verify>
    <automated>powershell -NoProfile -Command '$lt=[char]60; rg -n "(($lt)/?v-[a-z]|($lt)style|lang=\"scss\"|\.scss|:deep\(\.v-)" client/app/components/dashboard/TeacherWorkspaceDashboard.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }'</automated>
    <automated>powershell -NoProfile -Command "$env:Path = ""$env:LOCALAPPDATA\nvm\v24.11.1;$env:Path""; npm.cmd --prefix client run lint"</automated>
  </verify>
  <done>Teacher dashboard upcoming sessions, mini calendar, action items, and empty state are behavior-compatible, responsive, and marker-clean.</done>
</task>

</tasks>

<risks>
- Teacher dashboard has many style-class dependencies; translate layout/state classes before removing CSS import.
- Hero can become too marketing-like; keep app-surface density and 24px max heading per UI spec.
- Do not touch dashboard data/composable ownership or backend APIs.
</risks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboard data to UI | Existing dashboard/session data is rendered into teacher workflow panels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information disclosure | teacher dashboard | mitigate | Preserve existing data sources and tenant-scoped composables; do not add API calls in UI primitives. |
| T-03-04-02 | Denial of service | responsive dashboard layout | mitigate | Use stable min heights and responsive one-column stacking; verify mobile no-overlap. |
| T-03-04-SC | Tampering | package installs | accept | No package install is planned. |
</threat_model>

<verification>
Run targeted marker scan, client lint/typecheck, and visual QA for teacher dashboard with next session, empty upcoming sessions, desktop width, and mobile-width stacking.
</verification>

<success_criteria>
Teacher dashboard is Tailwind/UI-kit based, preserves calendar CTA and current data display, passes targeted scans, and has APP-05 visual parity evidence.
</success_criteria>

<output>
Create `.planning/phases/03-app-shell-shared-surface-migration/03-04-SUMMARY.md` when done.
</output>
