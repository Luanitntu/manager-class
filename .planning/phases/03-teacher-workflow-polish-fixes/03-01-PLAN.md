---
phase: 03-teacher-workflow-polish-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/layouts/default.vue
  - client/app/composables/useProfile.ts
  - client/app/composables/useSnackbar.ts
  - server/src/modules/dashboard/dashboard.service.ts
  - client/app/pages/dashboard.vue
  - client/app/pages/calendar.vue
  - client/app/components/SessionDialog.vue
  - client/app/pages/classes.vue
  - client/app/pages/students.vue
  - client/app/pages/assistants.vue
  - client/app/pages/documents.vue
  - client/app/pages/payments.vue
  - client/app/pages/reports.vue
  - client/app/pages/profile.vue
  - client/app/pages/audit-logs.vue
autonomous: true
requirements: [UI-04, DATA-01, DATA-02, DATA-03, BUG-03, BUG-04]
user_setup: []
must_haves:
  truths:
    - "Every teacher-facing page must adopt AppPageHeader and AppState."
    - "Teacher dashboard features live data loading on open, 7-day upcoming session window, and a manual refresh button."
    - "Calendar events are color-coded by class, using class colors with a fallback hashed from class ID."
    - "A unified snackbar notification pattern is established and used for all CRUD notifications (green for success, red for error)."
    - "SessionDialog.vue features inline validation rules for required inputs, chronological start/end times, and valid recurring date ranges."
    - "Profile page calls the new useProfile composable rather than making direct useApi().request calls."
  artifacts:
    - path: "client/app/composables/useSnackbar.ts"
      provides: "Unified snackbar state management"
    - path: "client/app/composables/useProfile.ts"
      provides: "Profile queries and mutations wrapping TanStack Query/Mutation"
---

<objective>
Refine and polish the teacher workflows and pages using the Phase 2 visual system, and fix key data-display bugs (including profile direct API calls, dashboard refresh/7-day window, calendar color coding, and form validation).
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-teacher-workflow-polish-fixes/03-CONTEXT.md
@.planning/phases/03-teacher-workflow-polish-fixes/03-RESEARCH.md
@client/app/layouts/default.vue
@client/app/pages/dashboard.vue
@client/app/pages/calendar.vue
@client/app/components/SessionDialog.vue
@client/app/pages/classes.vue
@client/app/pages/students.vue
@client/app/pages/assistants.vue
@client/app/pages/documents.vue
@client/app/pages/payments.vue
@client/app/pages/reports.vue
@client/app/pages/profile.vue
@client/app/pages/audit-logs.vue
@server/src/modules/dashboard/dashboard.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create global useSnackbar composable and default layout integration</name>
  <files>client/app/composables/useSnackbar.ts, client/app/layouts/default.vue</files>
  <read_first>client/app/layouts/default.vue</read_first>
  <action>Create a centralized `useSnackbar` composable to manage global snackbar states. In `default.vue`, import the snackbar state and place a single, styled `<v-snackbar>` wrapper with support for success/error/warning colors, close buttons, and correct typography.</action>
  <verify>rg -n "useSnackbar" client/app/layouts/default.vue</verify>
  <acceptance_criteria>
    - Centralized snackbar state exists under `client/app/composables/useSnackbar.ts`.
    - `default.vue` layout renders the global snackbar properly.
  </acceptance_criteria>
  <done>Global snackbar system is ready for CRUD operations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement useProfile composable and refactor profile page</name>
  <files>client/app/composables/useProfile.ts, client/app/pages/profile.vue</files>
  <read_first>client/app/pages/profile.vue</read_first>
  <action>Create `client/app/composables/useProfile.ts` following existing composable patterns (exposing query for `/users/me/profile` and mutations for patch). Update `client/app/pages/profile.vue` to use this composable, adopt `AppPageHeader` and `AppState`, and trigger the global snackbar on save success or error.</action>
  <verify>rg -n "useProfile" client/app/pages/profile.vue</verify>
  <acceptance_criteria>
    - No direct API request in `profile.vue`.
    - Profile page adopts the Phase 2 visual headers, card styling, and snackbar.
  </acceptance_criteria>
  <done>Profile page uses useProfile composable and Phase 2 visual styles.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor dashboard refresh and upcoming sessions 7-day window</name>
  <files>server/src/modules/dashboard/dashboard.service.ts, client/app/pages/dashboard.vue</files>
  <read_first>server/src/modules/dashboard/dashboard.service.ts, client/app/pages/dashboard.vue</read_first>
  <action>Modify `dashboard.service.ts` to return all scheduled sessions in a 7-day window (now to 7 days from now) for teachers. Modify `dashboard.vue` to slot a refresh button in `AppPageHeader` actions that triggers the query refetch, and ensure the loading and error states handle correctly.</action>
  <verify>rg -n "lte|now" server/src/modules/dashboard/dashboard.service.ts</verify>
  <acceptance_criteria>
    - Teacher stats only include upcoming sessions within a 7-day range.
    - Dashboard page includes a manual refresh button.
  </acceptance_criteria>
  <done>Dashboard uses a 7-day upcoming range and live refetch.</done>
</task>

<task type="auto">
  <name>Task 4: Polish calendar event coloring and SessionDialog validation</name>
  <files>client/app/pages/calendar.vue, client/app/components/SessionDialog.vue</files>
  <read_first>client/app/pages/calendar.vue, client/app/components/SessionDialog.vue</read_first>
  <action>In `calendar.vue`, implement `getColorFromId(classId)` helper and color-code events dynamically using class colors. In `SessionDialog.vue`, wrap inputs in a `v-form` with standard inline validation rules (date/time constraints, class selection validation) and replace local error alerts with global snackbar notifications on save or delete success/errors.</action>
  <verify>rg -n "v-form|validate" client/app/components/SessionDialog.vue</verify>
  <acceptance_criteria>
    - Events are color-coded by class with reliable hashing fallback.
    - SessionDialog includes inline validation error messages.
    - Global snackbar notifications trigger on save/delete actions.
  </acceptance_criteria>
  <done>Calendar class coloring and SessionDialog validation polish are complete.</done>
</task>

<task type="auto">
  <name>Task 5: Refactor remaining teacher pages to adopt visual system and snackbars</name>
  <files>client/app/pages/classes.vue, client/app/pages/students.vue, client/app/pages/assistants.vue, client/app/pages/documents.vue, client/app/pages/payments.vue, client/app/pages/reports.vue, client/app/pages/audit-logs.vue</files>
  <read_first>client/app/pages/classes.vue, client/app/pages/students.vue, client/app/pages/assistants.vue, client/app/pages/documents.vue, client/app/pages/payments.vue, client/app/pages/reports.vue, client/app/pages/audit-logs.vue</read_first>
  <action>Update classes, students, assistants, documents, payments, reports, and audit-logs pages to:
- Use `AppPageHeader` for headers and action buttons.
- Use `AppState` for loading, error, and empty states.
- Trigger central `useSnackbar` notifications on CRUD actions (e.g., creating class, deleting documents, recording payments, etc.).</action>
  <verify>rg -n "AppPageHeader|AppState" client/app/pages/classes.vue client/app/pages/students.vue client/app/pages/documents.vue client/app/pages/payments.vue</verify>
  <acceptance_criteria>
    - All 7 listed pages adopt AppPageHeader and AppState.
    - All CRUD operations on these pages report success/error via global snackbar notifications.
  </acceptance_criteria>
  <done>All teacher pages are aligned with the Phase 2 visual system.</done>
</task>

</tasks>

<verification>
- Verify client builds and compiles correctly:
  - From `client/`, run `npm run lint`, `npm run typecheck`, and `npm run build`.
- Verify server tests compile and run:
  - From `server/`, run `npm run lint`, `npm run build`, and `npm run test` (or `npm test`).
</verification>

<success_criteria>
- The backend upcoming session query is updated to a 7-day window.
- The global snackbar system is fully integrated across all teacher pages and CRUD forms.
- All teacher pages (classes, students, assistants, documents, payments, reports, profile, audit-logs) conform to Phase 2 layout specifications.
- Hashed class-based colors are visible on calendar events.
- Form fields in SessionDialog display inline validation errors correctly.
</success_criteria>

<output>
Create `.planning/phases/03-teacher-workflow-polish-fixes/03-01-SUMMARY.md` after waves are complete.
</output>
