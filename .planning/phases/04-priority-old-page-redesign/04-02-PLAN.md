---
phase: 04-priority-old-page-redesign
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/pages/assistants/[id].vue
autonomous: true
requirements:
  - PAGE-02
  - PAGE-05
  - PAGE-06
requirements_addressed:
  - PAGE-02
  - PAGE-05
  - PAGE-06
must_haves:
  truths:
    - "D-05 PAGE-02: /assistants/[id] uses header plus two-column desktop summary; profile and salary summary stack on mobile; lower sections remain full width."
    - "D-06 PAGE-02: salary configuration remains visible inline with method, rate, effective date, and save action."
    - "D-07 PAGE-02: schedule, breakdown, and history stay table-based on mobile with horizontal overflow."
    - "D-08 PAGE-02: edit profile remains compact dialog for phone, level, hometown with loading and close behavior."
  artifacts:
    - path: "client/app/pages/assistants/[id].vue"
      provides: "Tailwind/shared-UI assistant detail route"
      contains: "UiTabs"
  key_links:
    - from: "client/app/pages/assistants/[id].vue"
      to: "client/app/composables/useAssistants.ts"
      via: "preserve detail, salary summary, sessions, and updateSalary composables"
      pattern: "useAssistantDetail|useAssistantSalarySummary|useAssistantSessions|useAssistantMutations"
---

<objective>
Redesign `/assistants/[id]` as a behavior-preserving assistant detail route with shared Tailwind components.

Purpose: Satisfy PAGE-02 and D-05..D-08 while keeping the existing assistant detail, salary, session, i18n, timezone, and mutation data flows.
Output: `client/app/pages/assistants/[id].vue` scan-clean for old route UI markers and ready for desktop/mobile visual QA.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-priority-old-page-redesign/04-CONTEXT.md
@.planning/phases/04-priority-old-page-redesign/04-UI-SPEC.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-MIGRATION-INVENTORY.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/app/components/ui/README.md
@client/app/pages/assistants/[id].vue
@client/app/composables/useAssistants.ts
@client/app/components/AppSkeleton.vue
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate header, profile summary, salary summary, and assigned classes</name>
  <files>client/app/pages/assistants/[id].vue</files>
  <behavior>
    - PAGE-02 D-05: assistant header includes back action, avatar, name, email, optional level badge, and edit profile action.
    - PAGE-02 D-05: profile and salary summary render side-by-side on desktop and stack on mobile.
    - PAGE-02 D-05: assigned classes remain full width with involvement merge and role/session labels.
  </behavior>
  <action>Replace top-level `div`, `v-btn`, `v-avatar`, `v-chip`, `v-row`, `v-col`, `v-card`, and `v-list` usage in the header/profile/salary/assigned-class sections with `UiPage`, `UiButton`, `UiAvatar`, `UiBadge`, Tailwind grid/flex, `UiCard`, `UiMetricCard`, `UiList`/row markup, `UiEmptyState`, and `AppIcon`, per D-05. Preserve `route.params.id`, `useAssistantDetail`, `useAssistantSalarySummary`, `useAssistantSessions`, `useAvatar`, `involvement`, `totalSessionsCount`, `roleLabel`, `money`, `fmtDate`, and i18n labels. Add loading detail skeleton before assistant data is present. Use compact icon rows for profile details and class links to `/classes/:id` where current behavior links classes.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='client/app/pages/assistants/[id].vue'; $c=Get-Content -LiteralPath $p -Raw; if ($c -notmatch 'UiPage' -or $c -notmatch 'UiMetricCard' -or $c -notmatch 'involvement' -or $c -notmatch '/classes/\\$\\{c\\.id\\}') { exit 1 }"</automated>
  </verify>
  <done>Header/profile/salary/assigned-class sections use Tailwind/shared primitives, preserve computed data and links, and cover loading plus empty assigned-class states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate inline salary configuration and edit profile dialog</name>
  <files>client/app/pages/assistants/[id].vue</files>
  <behavior>
    - PAGE-02 D-06: salary method/rate/effective date remain inline and save through `saveSalary()`.
    - PAGE-02 D-08: edit profile opens compact dialog, prefills from assistant data, saves through `saveProfile()`, and closes after success.
    - PAGE-02 D-06/D-08: mutation loading disables or loads relevant salary/profile controls via `updateSalary.isPending.value`.
  </behavior>
  <action>Replace salary configuration controls with `UiCard`, `UiSelect`, `UiInput`, and `UiButton`, per D-06. Keep salary form watcher, `payload(extra)`, `saveSalary()`, and method options. Label salary CTA `Save salary settings`. Preserve rate history visible below salary config using plain rows and `AppIcon`, not a dialog. Replace edit-profile `v-dialog` with compact `UiDialog size="sm"`, `UiInput` controls for phone/level/hometown, and footer `UiButton` actions, per D-08. Keep `openProfile()` prefill, `saveProfile()` mutation body, close behavior, and loading state. Label profile CTA `Save profile`.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='client/app/pages/assistants/[id].vue'; $c=Get-Content -LiteralPath $p -Raw; if ($c -notmatch 'Save salary settings' -or $c -notmatch 'Save profile' -or $c -notmatch 'UiDialog' -or $c -notmatch 'updateSalary\\.isPending\\.value') { exit 1 }"</automated>
  </verify>
  <done>Salary settings remain inline, rate history remains visible, edit profile remains a compact dialog, and all existing mutation logic is preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate schedule, breakdown, and history tabs and tables</name>
  <files>client/app/pages/assistants/[id].vue</files>
  <behavior>
    - PAGE-02 D-07: schedule, salary breakdown, and salary history remain table-based and horizontally scroll on mobile.
    - PAGE-02 D-05: tabs remain caller-owned with default `tab = 'schedule'`.
    - PAGE-02 D-07: empty table copies are `No sessions scheduled`, `No salary records yet`, and `No salary history yet`.
  </behavior>
  <action>Replace `v-tabs`, `v-window`, `v-window-item`, and `v-table` sections with `UiTabs` plus caller-owned conditional panels and `UiTable`, per D-07. Preserve `tab = 'schedule'`, status mapping, `dayOf`, `timeRange`, `fmtMonth`, `money`, table columns, footer totals, and all existing data arrays. Map status badges to `UiBadge` tones where `SCHEDULED=info`, `COMPLETED=success`, `CANCELLED=danger`. Keep tables inside the UI table horizontal-overflow wrapper; do not convert them into stacked cards. Use the specified empty copy for each table.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$lt=[char]60; rg -n \"(($lt)/?v-[a-z]|($lt)style|lang=`\"scss`\"|\.scss|:deep\(\.v-)\" client/app/pages/assistants/[id].vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>Detail tabs and tables use shared Tailwind primitives, preserve data/formatting, retain mobile horizontal overflow, and have no old route UI markers.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| browser to assistant API via composables | Route-owned salary/profile input reaches existing assistant mutation endpoint through `useAssistantMutations`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-03 | Tampering | salary/profile forms | mitigate | Preserve current `payload(extra)` and mutation facade; do not add tenant/id fields beyond route id. |
| T-04-04 | Information Disclosure | salary and session tables | mitigate | Render only data from existing detail/summary/session composables; no new backend query or cross-route data source. |
</threat_model>

<verification>
Automated route scan must pass for `client/app/pages/assistants/[id].vue`. Final Phase 4 verification plan reruns lint, typecheck, build, route scans, UI-kit boundary scan, and desktop/mobile visual QA.
</verification>

<success_criteria>
PAGE-02 is satisfied for `/assistants/[id]`; D-05, D-06, D-07, and D-08 are implemented; PAGE-05/PAGE-06 visual consistency requirements are ready for final QA.
</success_criteria>

<output>
Create `.planning/phases/04-priority-old-page-redesign/04-02-SUMMARY.md` when done.
</output>
