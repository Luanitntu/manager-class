---
phase: 04-priority-old-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/pages/assistants/index.vue
autonomous: true
requirements:
  - PAGE-01
  - PAGE-05
  - PAGE-06
requirements_addressed:
  - PAGE-01
  - PAGE-05
  - PAGE-06
must_haves:
  truths:
    - "D-01 PAGE-01: /assistants renders dense list rows, not a grid; avatar left, identity center, salary/class badges right on desktop and wrapping on mobile."
    - "D-02 PAGE-01: create-assistant remains compact UiDialog around 460px with current fields, inline error, loading, and disabled validation."
    - "D-03 PAGE-01: assistant empty state is compact inside the list surface with Create assistant CTA."
    - "D-04 PAGE-01: search and limit reset page to 1; pagination remains TablePager/UiPagination compatible; no debounce added."
  artifacts:
    - path: "client/app/pages/assistants/index.vue"
      provides: "Tailwind/shared-UI /assistants route"
      contains: "UiPage"
  key_links:
    - from: "client/app/pages/assistants/index.vue"
      to: "client/app/composables/useAssistants.ts"
      via: "preserve useAssistants(search, page, limit) and useAssistantMutations createAssistant"
      pattern: "useAssistants\\(search, page, limit\\)"
---

<objective>
Redesign `/assistants` as a vertical route slice using the shared Tailwind UI kit while preserving assistant search, pagination, create, list, and detail-open behavior.

Purpose: Satisfy PAGE-01 and the D-01..D-04 assistant-list decisions without changing backend contracts or feature composables.
Output: `client/app/pages/assistants/index.vue` scan-clean for old route UI markers and visually aligned with the Phase 3 shell.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-priority-old-page-redesign/04-CONTEXT.md
@.planning/phases/04-priority-old-page-redesign/04-UI-SPEC.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-MIGRATION-INVENTORY.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/app/components/ui/README.md
@client/app/pages/assistants/index.vue
@client/app/composables/useAssistants.ts
@client/app/components/AssistantDetailDialog.vue
@client/app/components/TablePager.vue
@client/app/components/AppSkeleton.vue
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate assistant list route shell and rows</name>
  <files>client/app/pages/assistants/index.vue</files>
  <behavior>
    - PAGE-01 D-01: populated assistants render as dense clickable rows with avatar, full name, email, optional phone, salary badge, and class-count badge.
    - PAGE-01 D-03: empty data renders compact `UiEmptyState` inside the list/card area with `Create assistant` action.
    - PAGE-01 D-04: `watch([search, limit], () => page.value = 1)` remains and `TablePager` still binds page/limit/meta.
  </behavior>
  <action>Replace the route wrapper, header, toolbar search, list/card surface, assistant rows, badges, avatar, loading, empty state, and pagination with `UiPage`, `UiPageHeader`, `UiToolbar`, `UiInput`, `UiCard` or `UiList`, `UiListItem`, `UiAvatar`, `UiBadge`, `UiEmptyState`, `UiButton`, `AppIcon`, `AppSkeleton`, and `TablePager`, per D-01/D-03/D-04. Preserve `search`, `page`, `limit`, `useAssistants(search, page, limit)`, `openDetail(id)`, `AssistantDetailDialog v-model`, `selectedId`, `salaryLabel`, and `useAvatar`. Do not call APIs directly in template helpers or add backend changes. Ensure row text uses `min-w-0`, truncation/wrapping where needed, and no page-level horizontal scroll.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$lt=[char]60; rg -n \"(($lt)/?v-[a-z]|($lt)style|lang=`\"scss`\"|\.scss|:deep\(\.v-)\" client/app/pages/assistants/index.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>`/assistants` renders header/search/list/loading/empty/pagination/detail-open through Tailwind/shared primitives, preserves refs/composables/events, and has no old route UI markers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate create assistant dialog</name>
  <files>client/app/pages/assistants/index.vue</files>
  <behavior>
    - PAGE-01 D-02: create dialog remains compact and contains full name, email, temporary password, and optional phone fields.
    - PAGE-01 D-02: create action is disabled when full name/email are missing or password length is below 8.
    - PAGE-01 D-02: create loading, inline extracted error, close-on-success, and form reset behavior remain.
  </behavior>
  <action>Replace the create-assistant `v-dialog`/card/form/actions with `UiDialog size="sm"`, `UiInput`, `UiAlert tone="error"`, `UiActionGroup` or flex actions, and `UiButton`, per D-02. Keep `createOpen`, `error`, `form`, `createAssistant.isPending.value`, the disabled expression, `extractApiError(e) ?? 'Could not create assistant'`, success close, and form reset exactly behavior-compatible. Use CTA copy `Create assistant`; keep cancel behavior. Do not widen the dialog beyond the compact contract.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='client/app/pages/assistants/index.vue'; $c=Get-Content -LiteralPath $p -Raw; if ($c -notmatch 'UiDialog' -or $c -notmatch 'UiAlert' -or $c -notmatch 'form\.password\.length < 8' -or $c -notmatch 'extractApiError') { exit 1 }"</automated>
  </verify>
  <done>Create dialog uses shared Tailwind primitives, preserves validation/loading/error/reset behavior, and remains compact.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| browser to assistant API via composables | Untrusted form/search input crosses through existing `useAssistants` and `useAssistantMutations`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-01 | Tampering | create assistant form | mitigate | Preserve existing mutation facade and disabled guard; no direct API call or tenant parameter in route UI. |
| T-04-02 | Information Disclosure | assistant list rows | mitigate | Render only fields already returned by `useAssistants`; do not add new backend fields or cross-tenant filters. |
</threat_model>

<verification>
Automated route scan must pass for `client/app/pages/assistants/index.vue`. Final Phase 4 verification plan reruns lint, typecheck, build, route scans, UI-kit boundary scan, and desktop/mobile visual QA.
</verification>

<success_criteria>
PAGE-01 is satisfied for `/assistants`; D-01, D-02, D-03, and D-04 are implemented; PAGE-05/PAGE-06 visual consistency requirements are ready for final QA.
</success_criteria>

<output>
Create `.planning/phases/04-priority-old-page-redesign/04-01-SUMMARY.md` when done.
</output>
