---
phase: 04-priority-old-page-redesign
plan: 05
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
  - 04-03
  - 04-04
files_modified:
  - client/app/pages/assistants/index.vue
  - client/app/pages/assistants/[id].vue
  - client/app/pages/audit-logs.vue
  - client/app/pages/profile.vue
  - .planning/phases/04-priority-old-page-redesign/04-VERIFICATION.md
autonomous: false
requirements:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-06
requirements_addressed:
  - PAGE-01
  - PAGE-02
  - PAGE-03
  - PAGE-04
  - PAGE-05
  - PAGE-06
must_haves:
  truths:
    - "D-16 PAGE-06: targeted desktop and mobile visual QA covers /assistants, /assistants/[id], /audit-logs, and /profile."
    - "D-16 PAGE-06: frontend `npm run lint`, `npm run typecheck`, and `npm run build` run from `client/` and results are recorded."
    - "D-16 PAGE-06: QA notes explicitly cover no overflow, overlap, clipped content, missing loading/empty/error states, or broken controls."
    - "PAGE-05: all four priority pages match the newer app UI direction across spacing, typography, color, responsive behavior, and state treatment."
  artifacts:
    - path: ".planning/phases/04-priority-old-page-redesign/04-VERIFICATION.md"
      provides: "Phase 4 route scan, gate, and visual QA evidence"
      contains: "npm run build"
  key_links:
    - from: "Phase 4 route files"
      to: "client/app/components/ui"
      via: "static scan confirms no route old markers and UI kit remains data-agnostic"
      pattern: "rg route old markers and UI-kit boundary scan"
---

<objective>
Perform the final Phase 4 verification and sweep across the four redesigned priority routes.

Purpose: Prove PAGE-01 through PAGE-06 are complete, D-16 verification is satisfied, and the route files are clean without corrupting Phase 5/out-of-scope boundaries.
Output: passing client gates where environment allows, route/static scan evidence, desktop/mobile visual QA notes, and `.planning/phases/04-priority-old-page-redesign/04-VERIFICATION.md`.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-priority-old-page-redesign/04-CONTEXT.md
@.planning/phases/04-priority-old-page-redesign/04-UI-SPEC.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-MIGRATION-INVENTORY.md
@.planning/phases/03-app-shell-shared-surface-migration/03-VERIFICATION.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/app/components/ui/README.md
@client/app/pages/assistants/index.vue
@client/app/pages/assistants/[id].vue
@client/app/pages/audit-logs.vue
@client/app/pages/profile.vue
</context>

<tasks>
<task type="auto">
  <name>Task 1: Run route old-marker and UI-kit boundary scans</name>
  <files>client/app/pages/assistants/index.vue, client/app/pages/assistants/[id].vue, client/app/pages/audit-logs.vue, client/app/pages/profile.vue, .planning/phases/04-priority-old-page-redesign/04-VERIFICATION.md</files>
  <action>Run the Phase 4 old-marker scan over exactly the four route files for `<v-*`, `</v-*`, `<style>`, `lang="scss"`, `.scss`, and `:deep(.v-)`. If matches remain in those four files, make only the smallest route-file cleanup needed to remove them while preserving the relevant PAGE and D-XX behavior. Run the UI-kit boundary scan over `client/app/components/ui` for feature composables, auth stores, direct fetch, or `$fetch`; do not edit UI-kit files unless a Phase 4 executor introduced a boundary violation. Record commands, exit codes, and any cleanup in `04-VERIFICATION.md`. Do not fail Phase 4 for broader app old markers already deferred by `03-MIGRATION-INVENTORY.md`.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$lt=[char]60; rg -n \"(($lt)/?v-[a-z]|($lt)style|lang=`\"scss`\"|\.scss|:deep\(\.v-)\" client/app/pages/assistants/index.vue client/app/pages/assistants/[id].vue client/app/pages/audit-logs.vue client/app/pages/profile.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
    <automated>powershell -NoProfile -Command "rg -n \"(useApi|useAuthStore|useSessions|useStudents|useAssistants|useAuditLogs|useClasses|fetch\\(|`$fetch)\" client/app/components/ui; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>Four priority route files are old-marker clean; UI kit remains data-agnostic; evidence is recorded in `04-VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Run frontend gates from client</name>
  <files>.planning/phases/04-priority-old-page-redesign/04-VERIFICATION.md</files>
  <action>Run frontend gates from `client/`: `npm run lint`, `npm run typecheck`, and `npm run build`, per D-16. Record exact command, pass/fail, warnings, and blocker status in `04-VERIFICATION.md`. If a gate fails due to Phase 4 route changes, fix the route code in the owning file and rerun the failing gate. If a command cannot run in the environment, record the exact reason and closest verification performed. Do not run backend checks unless backend files were touched; Phase 4 plans should not touch backend files.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Push-Location client; npm.cmd run lint; $lint=$LASTEXITCODE; npm.cmd run typecheck; $type=$LASTEXITCODE; npm.cmd run build; $build=$LASTEXITCODE; Pop-Location; if ($lint -ne 0 -or $type -ne 0 -or $build -ne 0) { exit 1 }"</automated>
  </verify>
  <done>Client lint/typecheck/build have passing evidence or documented environment blockers with closest verification; backend is confirmed untouched or backend checks are run only if touched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Phase 4 redesigned `/assistants`, `/assistants/[id]`, `/audit-logs`, and `/profile` with Tailwind/shared UI primitives and preserved route data flows.</what-built>
  <how-to-verify>
    1. Open each route at desktop width and mobile width: `/assistants`, `/assistants/[id]` for a real assistant id, `/audit-logs`, and `/profile`.
    2. For `/assistants`, verify loading list, populated list, empty list/search, pagination, create dialog disabled/loading/error/success close, and detail dialog open. Confirm D-01 through D-04.
    3. For `/assistants/[id]`, verify loading detail, profile/salary summary desktop two-column and mobile stacked layout, assigned classes populated/empty, inline salary save loading, rate history, schedule/breakdown/history tables with horizontal overflow, and edit profile dialog. Confirm D-05 through D-08.
    4. For `/audit-logs`, verify loading skeleton, populated table, empty table, filter wrapping, clear filters, pagination, and action badge colors. Confirm D-09 through D-12.
    5. For `/profile`, verify loading skeleton, populated 520px card, disabled email, full name/phone, timezone select, role badge, save loading, inline success, and inline error. Confirm D-13 through D-15.
    6. Record in `04-VERIFICATION.md` whether D-16 passed: no overflow outside intentional table scroll, no overlap, no clipped controls, no unreadable text, no broken wrapping, no missing focus, no missing loading/empty/error/success states, and no broken controls.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| browser to existing API composables | Phase 4 routes preserve existing feature-composable boundaries for assistant, audit, and profile data. |
| route UI to shared UI kit | Route UI passes display-only data into data-agnostic primitives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-09 | Tampering | final sweep | mitigate | Keep cleanup scoped to the four route files unless a UI-kit boundary violation was introduced by Phase 4. |
| T-04-10 | Information Disclosure | visual QA and verification artifact | mitigate | Record UI state/gate evidence; do not paste secrets, tokens, or private account data into screenshots/notes. |
| T-04-11 | Repudiation | verification evidence | mitigate | `04-VERIFICATION.md` records exact commands, exit codes, route QA matrix, and any unresolved blockers. |
</threat_model>

<verification>
Required commands:

- `powershell -NoProfile -Command "$lt=[char]60; rg -n \"(($lt)/?v-[a-z]|($lt)style|lang=\`\"scss\`\"|\.scss|:deep\(\.v-)\" client/app/pages/assistants/index.vue client/app/pages/assistants/[id].vue client/app/pages/audit-logs.vue client/app/pages/profile.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"`
- `powershell -NoProfile -Command "rg -n \"(useApi|useAuthStore|useSessions|useStudents|useAssistants|useAuditLogs|useClasses|fetch\\(|\`$fetch)\" client/app/components/ui; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"`
- From `client/`: `npm run lint`
- From `client/`: `npm run typecheck`
- From `client/`: `npm run build`
- Human desktop/mobile visual QA for all four priority routes per D-16.
</verification>

<success_criteria>
PAGE-01 through PAGE-06 are satisfied; D-16 verification is recorded; four priority route files are old-marker clean; UI kit boundary scan is clean; client lint/typecheck/build pass or exact environment blockers are documented; desktop/mobile visual QA is approved.
</success_criteria>

<output>
Create `.planning/phases/04-priority-old-page-redesign/04-05-SUMMARY.md` and `.planning/phases/04-priority-old-page-redesign/04-VERIFICATION.md` when done.
</output>
