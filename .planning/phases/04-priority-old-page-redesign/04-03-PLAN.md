---
phase: 04-priority-old-page-redesign
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/pages/audit-logs.vue
autonomous: true
requirements:
  - PAGE-03
  - PAGE-05
  - PAGE-06
requirements_addressed:
  - PAGE-03
  - PAGE-05
  - PAGE-06
must_haves:
  truths:
    - "D-09 PAGE-03: audit filters remain a horizontal wrapping toolbar with action, entity type, from date, to date, and clear filters visible together."
    - "D-10 PAGE-03: audit logs remain a mobile horizontal-overflow table with When, Actor, Action, and Entity columns."
    - "D-11 PAGE-03: action badge mapping remains semantic: created success, updated info, deleted danger/error, recorded primary/info, fallback neutral."
    - "D-12 PAGE-03: loading remains table skeleton style and empty state is compact inside the table/card area."
  artifacts:
    - path: "client/app/pages/audit-logs.vue"
      provides: "Tailwind/shared-UI audit log route"
      contains: "UiTable"
  key_links:
    - from: "client/app/pages/audit-logs.vue"
      to: "client/app/composables/useAudit.ts"
      via: "preserve useAuditLogs filter refs and limit"
      pattern: "useAuditLogs\\(\\{ action, entityType, from, to, page \\}, limit\\)"
---

<objective>
Redesign `/audit-logs` with shared Tailwind components while preserving filter, table, pagination, loading, and empty behavior.

Purpose: Satisfy PAGE-03 and D-09..D-12 without altering audit API contracts or tenant-scoped backend behavior.
Output: `client/app/pages/audit-logs.vue` scan-clean for old route UI markers and ready for desktop/mobile visual QA.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-priority-old-page-redesign/04-CONTEXT.md
@.planning/phases/04-priority-old-page-redesign/04-UI-SPEC.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-MIGRATION-INVENTORY.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/app/components/ui/README.md
@client/app/pages/audit-logs.vue
@client/app/composables/useAudit.ts
@client/app/components/TablePager.vue
@client/app/components/AppSkeleton.vue
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate audit page header and filter toolbar</name>
  <files>client/app/pages/audit-logs.vue</files>
  <behavior>
    - PAGE-03 D-09: filters render in a wrapping toolbar, not a collapsed panel.
    - PAGE-03 D-09: clear filters sets action, entity type, from, and to to empty strings.
    - PAGE-03 D-09: filter/limit watcher resets page to 1.
  </behavior>
  <action>Replace route wrapper, title/subtitle, and `v-select`/`v-text-field`/`v-btn` filter row with `UiPage`, `UiPageHeader`, `UiToolbar`, `UiSelect`, `UiInput`, and `UiButton`, per D-09. Preserve `action`, `entityType`, `from`, `to`, `page`, `limit`, `watch([action, entityType, from, to, limit], () => page.value = 1)`, `useAuditLogs({ action, entityType, from, to, page }, limit)`, `actionOptions`, and `clearFilters()`. Keep Vietnamese option labels already present. Use compact toolbar widths that wrap cleanly on mobile and never hide filters behind a drawer.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='client/app/pages/audit-logs.vue'; $c=Get-Content -LiteralPath $p -Raw; if ($c -notmatch 'UiToolbar' -or $c -notmatch 'UiSelect' -or $c -notmatch 'clearFilters' -or $c -notmatch 'page\\.value = 1') { exit 1 }"</automated>
  </verify>
  <done>Audit filters use shared Tailwind primitives, preserve refs/watchers/clear behavior, and wrap responsively.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate audit table, badges, loading, empty state, and pagination</name>
  <files>client/app/pages/audit-logs.vue</files>
  <behavior>
    - PAGE-03 D-10: logs render in a table with When, Actor, Action, Entity columns and horizontal overflow on mobile.
    - PAGE-03 D-11: action badges use equivalent `UiBadge` tones.
    - PAGE-03 D-12: initial loading uses table skeleton and empty state uses compact `UiEmptyState` inside the card/table area.
  </behavior>
  <action>Replace `v-card`, `v-table`, and `v-chip` with `UiCard`, `UiTable`, `UiBadge`, `UiEmptyState`, `AppSkeleton`, and `TablePager`, per D-10/D-11/D-12. Change `actionColor`/`colorFor` to return UI badge tones: `CREATED=success`, `UPDATED=info`, `DELETED=danger`, `RECORDED=primary`, fallback `neutral`. Preserve `fmt`, actor fallback dash, role display in parentheses, entity type plus first 8 entity id chars, `logs`, `meta`, and pagination binding. Empty heading/body must be `No audit logs found` and `Adjust filters to view matching activity.`.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$lt=[char]60; rg -n \"(($lt)/?v-[a-z]|($lt)style|lang=`\"scss`\"|\.scss|:deep\(\.v-)\" client/app/pages/audit-logs.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>Audit data surface uses shared Tailwind primitives, preserves display/pagination/states, and has no old route UI markers.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| browser to audit API via composable | Filter values cross through `useAuditLogs` to the existing tenant-scoped audit endpoint. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-05 | Information Disclosure | audit table | mitigate | Preserve existing `useAuditLogs` facade and visible columns; do not add expanded old/new value rendering. |
| T-04-06 | Denial of Service | filters/pagination | accept | Existing query/pagination behavior remains unchanged; no debounce added per D-04-like route parity and no new polling. |
</threat_model>

<verification>
Automated route scan must pass for `client/app/pages/audit-logs.vue`. Final Phase 4 verification plan reruns lint, typecheck, build, route scans, UI-kit boundary scan, and desktop/mobile visual QA.
</verification>

<success_criteria>
PAGE-03 is satisfied for `/audit-logs`; D-09, D-10, D-11, and D-12 are implemented; PAGE-05/PAGE-06 visual consistency requirements are ready for final QA.
</success_criteria>

<output>
Create `.planning/phases/04-priority-old-page-redesign/04-03-SUMMARY.md` when done.
</output>
