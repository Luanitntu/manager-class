---
phase: 04-priority-old-page-redesign
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/pages/profile.vue
autonomous: true
requirements:
  - PAGE-04
  - PAGE-05
  - PAGE-06
requirements_addressed:
  - PAGE-04
  - PAGE-05
  - PAGE-06
must_haves:
  truths:
    - "D-13 PAGE-04: timezone uses simple `UiSelect` over the existing timezone list, not a searchable select."
    - "D-14 PAGE-04: profile remains a compact single-card form around 520px, not a wider two-column account page."
    - "D-15 PAGE-04: save feedback stays inline in the card via `UiAlert` success/error, with no toast behavior."
  artifacts:
    - path: "client/app/pages/profile.vue"
      provides: "Tailwind/shared-UI profile route"
      contains: "UiSelect"
  key_links:
    - from: "client/app/pages/profile.vue"
      to: "client/app/composables/useProfile.ts"
      via: "preserve useMyProfile/useProfileMutations and auth store update behavior"
      pattern: "useMyProfile|useProfileMutations|useAuthStore"
---

<objective>
Redesign `/profile` as a compact shared-Tailwind form while preserving profile load, edit, timezone, save, error, success, and auth-store update behavior.

Purpose: Satisfy PAGE-04 and D-13..D-15 without adding new profile features, dependencies, or backend work.
Output: `client/app/pages/profile.vue` scan-clean for old route UI markers and ready for desktop/mobile visual QA.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-priority-old-page-redesign/04-CONTEXT.md
@.planning/phases/04-priority-old-page-redesign/04-UI-SPEC.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-HANDOFF.md
@.planning/phases/02-tailwind-design-system-shared-ui-kit/02-UI-SPEC.md
@.planning/phases/03-app-shell-shared-surface-migration/03-MIGRATION-INVENTORY.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/app/components/ui/README.md
@client/app/pages/profile.vue
@client/app/composables/useProfile.ts
@client/app/stores/auth.ts
@client/app/components/AppSkeleton.vue
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Migrate profile route shell and compact form card</name>
  <files>client/app/pages/profile.vue</files>
  <behavior>
    - PAGE-04 D-14: route uses page header and a single 520px max-width card.
    - PAGE-04 D-13: timezone uses `UiSelect` with the existing `timezones` array.
    - PAGE-04 D-14: email remains disabled; full name and phone remain editable inputs; role remains a badge.
  </behavior>
  <action>Replace route wrapper, title/subtitle, skeleton, `v-card`, `v-text-field`, `v-autocomplete`, and `v-chip` with `UiPage`, `UiPageHeader`, `AppSkeleton`, `UiCard`, `UiInput`, `UiSelect`, `UiBadge`, and `UiButton`, per D-13/D-14. Preserve `useAuthStore`, `useMyProfile`, `useProfileMutations`, `detectBrowserTimezone`, `timezones` fallback logic, `form`, and watcher that copies loaded profile data and updates `auth.user.avatarKey`. Keep the card `max-w-[520px]`; do not add a second column.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$p='client/app/pages/profile.vue'; $c=Get-Content -LiteralPath $p -Raw; if ($c -notmatch 'UiSelect' -or $c -match 'v-autocomplete' -or $c -notmatch 'max-w-\\[520px\\]' -or $c -notmatch 'auth\\.user\\.avatarKey') { exit 1 }"</automated>
  </verify>
  <done>Profile page uses shared Tailwind primitives, keeps the existing form model and profile watcher, and preserves compact 520px layout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate inline save feedback and save action</name>
  <files>client/app/pages/profile.vue</files>
  <behavior>
    - PAGE-04 D-15: save error renders inline `UiAlert tone="error"` with existing error text.
    - PAGE-04 D-15: save success renders inline `UiAlert tone="success"` copy `Profile updated.`
    - PAGE-04: save guard, mutation body, loading disabled state, and auth user fullName/timezone updates remain.
  </behavior>
  <action>Replace profile `v-alert` and save `v-btn` with `UiAlert` and `UiButton`, per D-15. Keep `save()` guard on `loading.value`, reset of `error`/`saved`, `updateProfile.mutateAsync` body, auth-store updates for `fullName` and `timezone`, fallback `extractApiError(e) ?? 'Could not save profile'`, and `finally` loading reset. Button label must be `Save profile`; do not add toast behavior.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$lt=[char]60; rg -n \"(($lt)/?v-[a-z]|($lt)style|lang=`\"scss`\"|\.scss|:deep\(\.v-)\" client/app/pages/profile.vue; if ($LASTEXITCODE -eq 0) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <done>Profile save feedback is inline through `UiAlert`, save action is behavior-compatible, CTA copy is `Save profile`, and route file has no old UI markers.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| browser to profile API via composable | User profile fields cross through existing `useProfileMutations`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-04-07 | Tampering | profile form | mitigate | Preserve existing mutation body fields only: fullName, phone, timezone. |
| T-04-08 | Spoofing | auth store profile display | mitigate | Preserve successful auth-store update only after `updateProfile.mutateAsync` resolves. |
</threat_model>

<verification>
Automated route scan must pass for `client/app/pages/profile.vue`. Final Phase 4 verification plan reruns lint, typecheck, build, route scans, UI-kit boundary scan, and desktop/mobile visual QA.
</verification>

<success_criteria>
PAGE-04 is satisfied for `/profile`; D-13, D-14, and D-15 are implemented; PAGE-05/PAGE-06 visual consistency requirements are ready for final QA.
</success_criteria>

<output>
Create `.planning/phases/04-priority-old-page-redesign/04-04-SUMMARY.md` when done.
</output>
