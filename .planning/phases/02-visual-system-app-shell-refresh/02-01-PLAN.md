---
phase: 02-visual-system-app-shell-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/vuetify.config.ts
  - client/app/assets/css/main.css
  - client/app/components/AppState.vue
  - client/app/components/AppPageHeader.vue
autonomous: true
requirements: [UI-01, UI-05]
user_setup: []
must_haves:
  truths:
    - "Vuetify remains the design foundation; no new UI registry or component framework is introduced."
    - "Theme tokens match the approved UI-SPEC palette, spacing, typography, border, and radius direction."
    - "Reusable states distinguish loading, empty, error, forbidden, and success so Phase 1 data-display bugs are not hidden."
    - "Decision coverage: D-09 through D-16 are translated into theme, CSS, and state component tasks."
  artifacts:
    - path: "client/vuetify.config.ts"
      provides: "Centralized visual system tokens and Vuetify defaults"
    - path: "client/app/assets/css/main.css"
      provides: "Global CSS variables, content width, and responsive primitives"
    - path: "client/app/components/AppState.vue"
      provides: "Reusable loading/empty/error/forbidden/success display pattern"
    - path: "client/app/components/AppPageHeader.vue"
      provides: "Consistent route heading and action layout"
  key_links:
    - from: "client/app/components/AppState.vue"
      to: ".planning/phases/02-visual-system-app-shell-refresh/02-UI-SPEC.md"
      via: "State component implements the UI-SPEC state/copy contract."
---

<objective>
Establish the shared visual-system foundation for Phase 2.

Purpose: Create reusable theme, spacing, typography, and state patterns that later shell/page work can adopt without ad-hoc styling.
Output: Updated Vuetify theme/global CSS plus reusable `AppState.vue` and `AppPageHeader.vue` components.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/02-visual-system-app-shell-refresh/02-CONTEXT.md
@.planning/phases/02-visual-system-app-shell-refresh/02-UI-SPEC.md
@.planning/phases/02-visual-system-app-shell-refresh/02-RESEARCH.md
@.planning/phases/01-audit-data-flow-baseline/01-AUDIT-SUMMARY.md
@client/vuetify.config.ts
@client/app/assets/css/main.css
@client/app/components/PlaceholderPage.vue
@client/app/utils/error.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh Vuetify theme tokens and defaults</name>
  <files>client/vuetify.config.ts</files>
  <read_first>client/vuetify.config.ts, .planning/phases/02-visual-system-app-shell-refresh/02-UI-SPEC.md</read_first>
  <action>Update the Vuetify theme to match the UI-SPEC palette: app background, surface, primary blue, teal/green, orange, info, warning, destructive/error, text, and border direction. Keep Vuetify as the only component library. Set component defaults for VCard, VBtn, fields, VDialog, VTable/VDataTable if available, and VChip so the app gets light radius, thin borders, subtle elevation, moderate density, and non-uppercase button labels.</action>
  <verify>rg -n "#2563EB|#14B8A6|#F97316|#F6F8FC|VCard|VBtn|VTextField" client/vuetify.config.ts</verify>
  <acceptance_criteria>
    - `client/vuetify.config.ts` includes the approved blue/teal-green/orange palette direction.
    - Defaults preserve moderate density and light radius/border treatment.
    - No new UI library or registry dependency is introduced.
  </acceptance_criteria>
  <done>Theme defaults are centralized and aligned with UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 2: Add global layout and typography primitives</name>
  <files>client/app/assets/css/main.css</files>
  <read_first>client/app/assets/css/main.css, .planning/phases/02-visual-system-app-shell-refresh/02-UI-SPEC.md</read_first>
  <action>Extend `main.css` with CSS variables for content width, app spacing, nav sizing, border color, text colors, and mobile safe-area/bottom-nav spacing. Keep `--st-content-max: 1400px`. Add small utility classes only when they represent stable app primitives, not one-off page styling.</action>
  <verify>rg -n -- "--st-content-max|--st-space|--st-border|--st-bottom-nav|safe-area|1400px" client/app/assets/css/main.css</verify>
  <acceptance_criteria>
    - Existing `1400px` content max width remains.
    - Mobile-safe bottom nav/content spacing variables exist.
    - Typography does not scale with viewport width.
  </acceptance_criteria>
  <done>Global CSS primitives support shell and responsive work.</done>
</task>

<task type="auto">
  <name>Task 3: Create reusable page header and state components</name>
  <files>client/app/components/AppPageHeader.vue, client/app/components/AppState.vue</files>
  <read_first>client/app/components/PlaceholderPage.vue, client/app/pages/dashboard.vue, client/app/utils/error.ts, .planning/phases/02-visual-system-app-shell-refresh/02-UI-SPEC.md</read_first>
  <action>Create `AppPageHeader.vue` for consistent route title/subtitle/actions. Create `AppState.vue` as a visual-only component supporting variants `loading`, `empty`, `error`, `forbidden`, and `success`, with optional icon/heading/body/action label/action event. Loading should support skeleton/top-progress presentation. Empty states should be friendly and lightly illustrated using MDI/icons or simple original markup; do not add external image assets unless already present.</action>
  <verify>rg -n "defineProps|loading|empty|error|forbidden|success|AppPageHeader|AppState" client/app/components/AppPageHeader.vue client/app/components/AppState.vue</verify>
  <acceptance_criteria>
    - State variants are explicit and not collapsed into a generic empty state.
    - Components are visual-only and do not call APIs directly.
    - Props and events are typed enough for Nuxt/Vue typecheck.
  </acceptance_criteria>
  <done>Reusable visual-state primitives are ready for shell/page adoption.</done>
</task>

</tasks>

<verification>
- [ ] `rg -n "#2563EB|#14B8A6|#F97316" client/vuetify.config.ts`
- [ ] `rg -n "loading|empty|error|forbidden|success" client/app/components/AppState.vue`
- [ ] When Node/npm are available: from `client/`, run `npm run lint` and `npm run typecheck`.
</verification>

<success_criteria>
- Theme and global CSS reflect the UI-SPEC contract.
- Reusable state/header components exist and avoid direct API calls.
- UI-01 and UI-05 have an implementation foundation before shell/page work starts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-system-app-shell-refresh/02-01-SUMMARY.md`.
</output>
