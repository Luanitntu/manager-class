---
phase: 02-visual-system-app-shell-refresh
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - client/app/layouts/default.vue
  - client/app/middleware/auth.global.ts
  - client/app/pages/index.vue
  - client/app/pages/login.vue
  - client/app/utils/navigation.ts
autonomous: true
requirements: [UI-01, UI-03]
user_setup: []
must_haves:
  truths:
    - "App shell navigation is role-aware and hides unavailable items rather than disabling irrelevant routes."
    - "Teacher default landing remains `/calendar`; student default landing becomes `/dashboard`."
    - "Mobile primary navigation uses a bottom nav with four to five role-specific items."
    - "Topbar shows user identity, role, profile/logout, and only safe existing-flow quick actions."
    - "Decision coverage: D-01 through D-08 are translated into shell, nav, quick-action, and route-default tasks."
  artifacts:
    - path: "client/app/utils/navigation.ts"
      provides: "Role-aware nav/default-route helper"
    - path: "client/app/layouts/default.vue"
      provides: "Refreshed desktop shell, topbar, grouped nav, mobile bottom nav"
    - path: "client/app/middleware/auth.global.ts"
      provides: "Role-aware redirect behavior for authenticated public-route visits"
  key_links:
    - from: "client/app/layouts/default.vue"
      to: "client/app/stores/auth.ts"
      via: "Layout reads current role/user to compute nav, topbar role label, and quick action."
---

<objective>
Refresh the shared app shell with role-aware navigation and route defaults.

Purpose: Make teacher/student navigation clear, modern, and aligned with the Phase 2 UI contract while preserving the teacher calendar-first workflow.
Output: Role-aware shell, mobile bottom nav, topbar identity/quick action, and default-route helper.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-visual-system-app-shell-refresh/02-CONTEXT.md
@.planning/phases/02-visual-system-app-shell-refresh/02-UI-SPEC.md
@.planning/phases/02-visual-system-app-shell-refresh/02-RESEARCH.md
@client/app/layouts/default.vue
@client/app/stores/auth.ts
@client/app/middleware/auth.global.ts
@client/app/pages/login.vue
@client/app/pages/index.vue
@client/app/components/AppPageHeader.vue
@client/app/components/AppState.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add role-aware navigation helper</name>
  <files>client/app/utils/navigation.ts</files>
  <read_first>client/app/stores/auth.ts, .planning/phases/02-visual-system-app-shell-refresh/02-CONTEXT.md, .planning/phases/02-visual-system-app-shell-refresh/02-UI-SPEC.md</read_first>
  <action>Create `client/app/utils/navigation.ts` with typed role-aware navigation groups, mobile-primary item selection, role labels, default route resolver, and safe quick-action metadata. Include teacher, student, assistant, and super-admin handling enough to avoid regressions, but keep teacher/student priority. Do not add center role. Do not include student scores/comments route unless an existing route exists; if absent, omit it or mark only as future in comments-free config.</action>
  <verify>rg -n "TEACHER|STUDENT|ASSISTANT|SUPER_ADMIN|getDefaultRoute|getNavigation|mobile|quick" client/app/utils/navigation.ts</verify>
  <acceptance_criteria>
    - Teacher default route resolves to `/calendar`.
    - Student default route resolves to `/dashboard`.
    - Student nav does not include teacher-only Students/Assistants/Reports/Audit Logs.
    - Center role is not introduced.
  </acceptance_criteria>
  <done>Navigation rules are centralized and role-aware.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh desktop shell and topbar</name>
  <files>client/app/layouts/default.vue</files>
  <read_first>client/app/layouts/default.vue, client/app/utils/navigation.ts, client/app/components/AppPageHeader.vue, client/app/assets/css/main.css</read_first>
  <action>Update `default.vue` to use grouped role-aware desktop navigation, subtle active states, refreshed brand block, role label in topbar, profile/logout menu, and one safe quick action when available. Keep all route links existing. Do not add data fetching in the layout. Use Vuetify components and the CSS/theme primitives from Plan 02-01.</action>
  <verify>rg -n "getNavigation|role|quick|bottom|v-navigation-drawer|v-bottom-navigation|logout" client/app/layouts/default.vue</verify>
  <acceptance_criteria>
    - Desktop sidebar groups items by work area.
    - Topbar shows user name and role context.
    - Quick action is role-aware and reuses existing route/flow only.
    - Active state is clear but light, not a heavy filled pill.
  </acceptance_criteria>
  <done>Desktop shell and topbar match the UI-SPEC shell contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add mobile bottom navigation</name>
  <files>client/app/layouts/default.vue, client/app/assets/css/main.css</files>
  <read_first>client/app/layouts/default.vue, client/app/utils/navigation.ts, client/app/assets/css/main.css</read_first>
  <action>Add mobile bottom navigation for primary role-specific routes, capped at four or five items. Ensure the main content has bottom padding so controls are not covered. Keep drawer/menu available for secondary items if needed. Use Vuetify display utilities or reactive display support consistent with the project.</action>
  <verify>rg -n "v-bottom-navigation|mobile|bottom|safe-area|st-bottom" client/app/layouts/default.vue client/app/assets/css/main.css</verify>
  <acceptance_criteria>
    - Mobile primary nav does not require opening a drawer.
    - Bottom nav has no more than five primary items per role.
    - Content is not hidden behind the bottom nav.
  </acceptance_criteria>
  <done>Mobile shell navigation is usable and role-aware.</done>
</task>

<task type="auto">
  <name>Task 4: Align auth/index role redirects</name>
  <files>client/app/middleware/auth.global.ts, client/app/pages/index.vue, client/app/pages/login.vue</files>
  <read_first>client/app/middleware/auth.global.ts, client/app/pages/index.vue, client/app/pages/login.vue, client/app/utils/navigation.ts</read_first>
  <action>Update default-route behavior to use the centralized helper where appropriate. Authenticated teachers should route to `/calendar`; students should route to `/dashboard`. Preserve redirect query behavior for unauthenticated users. Avoid broad auth refactors.</action>
  <verify>rg -n "getDefaultRoute|/calendar|/dashboard|redirect" client/app/middleware/auth.global.ts client/app/pages/index.vue client/app/pages/login.vue</verify>
  <acceptance_criteria>
    - Login/register redirect for authenticated students no longer always lands on `/calendar`.
    - Teacher calendar-first behavior remains intact.
    - Existing redirect query behavior remains intact.
  </acceptance_criteria>
  <done>Role default routing matches Phase 2 decisions.</done>
</task>

</tasks>

<verification>
- [ ] `rg -n "getDefaultRoute|STUDENT|TEACHER" client/app/utils/navigation.ts client/app/middleware/auth.global.ts client/app/pages/index.vue client/app/pages/login.vue`
- [ ] `rg -n "v-bottom-navigation|quick|role" client/app/layouts/default.vue`
- [ ] When Node/npm are available: from `client/`, run `npm run lint`, `npm run typecheck`, and `npm run build`.
</verification>

<success_criteria>
- UI-03 is covered by role-aware desktop/mobile shell behavior.
- Student and teacher default routes match the locked decisions.
- Shell remains visual/navigation-only and does not introduce new data queries or center scope.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-system-app-shell-refresh/02-02-SUMMARY.md`.
</output>
