---
phase: 01-styling-platform-cutover
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/assets/css/main.css
  - client/app/styles/index.scss
  - client/app/styles/index.css
  - client/app/styles/landing.scss
  - client/app/styles/landing.css
  - client/app/styles/login.scss
  - client/app/styles/login.css
  - client/app/styles/_auth-shared.scss
  - client/app/styles/_auth-shared.css
  - client/app/pages/index.vue
autonomous: true
requirements:
  - STYLE-02
  - STYLE-03
requirements_addressed:
  - STYLE-02
  - STYLE-03
must_haves:
  truths:
    - "Global/auth/landing build-path styles no longer require Sass per D-04 and D-05."
    - "Auth and landing visuals keep their current intent without page redesign per D-03."
    - "Non-blocking old UI tags or visual redesign work found while scanning are recorded for Plan 07 inventory per D-06."
  artifacts:
    - path: "client/app/assets/css/main.css"
      provides: "Tailwind entry without global SCSS import"
    - path: "client/app/styles/index.css"
      provides: "Plain CSS replacement for global build-path styles where still needed"
    - path: "client/app/styles/login.css"
      provides: "Plain CSS replacement for login/auth styles where still needed"
    - path: "client/app/styles/landing.css"
      provides: "Plain CSS replacement for landing styles where still needed"
    - path: "client/app/pages/index.vue"
      provides: "Landing page imports plain CSS or component-local CSS"
  key_links:
    - from: "client/app/assets/css/main.css"
      to: "client/app/styles/index.css"
      via: "CSS import only if global rules still need a separate file"
      pattern: "index.css"
    - from: "client/app/pages/index.vue"
      to: "client/app/styles/landing.css"
      via: "Landing SFC imports plain CSS or uses component-local plain CSS"
      pattern: "landing.css"
---

<objective>
Convert only the global/auth/landing SCSS build blockers to Tailwind-compatible CSS.

Purpose: Phase 1 removes Sass from the current global/auth/landing build path while preserving visual intent per D-03, D-04, D-05, and D-06.
Output: Plain CSS or component-local plain CSS for global/auth/landing surfaces; register styles move to Plan 03; no dashboard, calendar, shell, or page redesign work in this plan.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-styling-platform-cutover/01-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/app/assets/css/main.css
@client/app/styles/index.scss
@client/app/styles/landing.scss
@client/app/styles/login.scss
@client/app/styles/_auth-shared.scss
@client/app/pages/index.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove global SCSS import and preserve tokens</name>
  <files>client/app/assets/css/main.css, client/app/styles/index.scss, client/app/styles/index.css</files>
  <action>Remove the global SCSS import from `main.css` per D-04. Convert only still-required global rules to plain CSS or fold them into `main.css`, flattening Sass nesting and replacing Sass-only syntax with valid CSS. Keep the Tailwind 4 entry, `@theme`, tokens, font, keyframes, and `.st-content` behavior. Do not touch register, dashboard, calendar, shell, non-blocking old UI tags, or visual page redesign work per D-03 and D-06.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>`main.css` has no SCSS import; global build-path styles compile as plain CSS or are folded into `main.css`; register/dashboard/calendar/shell files are untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert auth and landing style entrypoints</name>
  <files>client/app/styles/login.scss, client/app/styles/login.css, client/app/styles/_auth-shared.scss, client/app/styles/_auth-shared.css, client/app/styles/landing.scss, client/app/styles/landing.css, client/app/pages/index.vue</files>
  <action>Convert auth/login/shared auth and landing SCSS build-path rules to plain CSS or component-local plain CSS per D-05. Preserve selectors and visual intent, flatten nesting, replace Sass variables/mixins with existing CSS variables or repeated CSS, update `index.vue` style imports or style attributes, and remove obsolete SCSS imports. Keep this limited to direct auth/landing Sass build blockers per D-03 and D-06.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>Auth/login/shared auth and landing style files used by Nuxt are plain CSS or removed because equivalent component-local plain CSS exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Scan only global/auth/landing SCSS blockers</name>
  <files>client/app/assets/css/main.css, client/app/styles/index.css, client/app/styles/login.css, client/app/styles/_auth-shared.css, client/app/styles/landing.css, client/app/pages/index.vue</files>
  <action>Run a targeted scan over only files touched in this plan plus their direct style imports. Fix SCSS-only direct build blockers in this slice. Do not repair register, dashboard, calendar, shell, non-blocking v-prefixed component usage, or lower-traffic visual drift here; record those findings for Plan 07 inventory per D-06, D-10, D-11, and D-12.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>Global/auth/landing files in this plan contain no direct SCSS build blockers; unrelated non-blocking migration findings are left for the detailed inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS selectors -> auth/landing DOM | Style migration changes user-facing auth and landing layout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Denial of Service | Client build | mitigate | Remove Sass-only syntax from global/auth/landing build path before dependency removal. |
| T-01-02-02 | Information Disclosure | Auth/landing styling | accept | CSS-only change does not expose data; preserve existing DOM and composable behavior. |
| T-01-02-SC | Tampering | npm installs | accept | No package install in this plan. |
</threat_model>

<verification>
Run from `client/`: `npm run typecheck`. Full platform scans, lint, typecheck, and build are enforced in Plan 07.
</verification>

<success_criteria>
Global/auth/landing SCSS blockers are converted to Tailwind-compatible CSS without redesigning product pages or touching register/dashboard/calendar/shell surfaces.
</success_criteria>

<output>
Create `.planning/phases/01-styling-platform-cutover/01-02-SUMMARY.md` when done.
</output>
