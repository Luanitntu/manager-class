---
phase: 01-styling-platform-cutover
plan: 06
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - client/app/layouts/default.vue
  - client/app/layouts/auth.vue
autonomous: true
requirements:
  - STYLE-03
  - STYLE-04
requirements_addressed:
  - STYLE-03
  - STYLE-04
must_haves:
  truths:
    - "Teacher/student shell renders without Vuetify shell primitives per D-02."
    - "Teacher calendar remains a primary navigation target and quick action."
    - "Auth layout renders without Vuetify."
    - "Shell migration does not redesign product pages per D-03."
  artifacts:
    - path: "client/app/layouts/default.vue"
      provides: "Tailwind teacher/student shell"
      contains: "useViewport"
    - path: "client/app/layouts/auth.vue"
      provides: "Tailwind auth layout wrapper"
  key_links:
    - from: "client/app/layouts/default.vue"
      to: "client/app/composables/useViewport.ts"
      via: "Viewport state controls mobile drawer behavior"
      pattern: "useViewport"
    - from: "client/app/layouts/default.vue"
      to: "client/app/components/AppIcon.vue"
      via: "All shell icons render through AppIcon"
      pattern: "AppIcon"
---

<objective>
Replace the default and auth shell Vuetify surface with Tailwind/Vue markup.

Purpose: The Nuxt module cannot be removed safely while the default/auth layouts depend on Vuetify app, drawer, bar, menu, list, alert, avatar, image, chip, badge, button, and icon primitives.
Output: Tailwind shell layouts preserving role-aware navigation, announcements, profile menu, logout, language switcher, and calendar-first teacher access.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-styling-platform-cutover/01-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/app/layouts/default.vue
@client/app/layouts/auth.vue
@client/app/components/AppIcon.vue
@client/app/composables/useViewport.ts
@client/app/components/LanguageSwitcher.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate default layout shell</name>
  <files>client/app/layouts/default.vue</files>
  <action>Rewrite `default.vue` to remove Vuetify shell primitives per D-02. Use semantic Tailwind/plain Vue markup for fixed/overlay side nav, top bar, mobile menu button, role-aware nav groups, active `NuxtLink` states, teacher/student variants, announcement banner, notification button, calendar quick action, profile dropdown, avatar image/fallback, and logout. Replace `useDisplay` with `useViewport`; replace icons with `AppIcon`; preserve `auth`, `logout`, `t`, `publicSettings`, `avatarSrc`, student/teacher branch behavior, and calendar prominence. Keep product page content slot unchanged and avoid page redesign per D-03.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>`default.vue` has no Vuetify imports or component tags; teacher calendar remains visible in daily navigation and quick action; mobile drawer opens/closes using Tailwind/Vue state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate auth layout wrapper</name>
  <files>client/app/layouts/auth.vue</files>
  <action>Rewrite `auth.vue` from Vuetify app/main wrappers to Tailwind/plain markup while preserving the language switcher position and slot rendering. Keep the existing `bg-surface` visual intent using Tailwind or CSS variables.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>`auth.vue` renders without Vuetify and keeps `LanguageSwitcher` in the top-right overlay position.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Check shell responsiveness and blocker tags</name>
  <files>client/app/layouts/default.vue, client/app/layouts/auth.vue</files>
  <action>Inspect desktop and mobile-width shell behavior in code and with available local dev/build tools. Fix obvious overflow, clipped text, or broken mobile drawer behavior inside the two layout files. Do not modify route pages, non-blocking old UI tags outside these layouts, or redesign work in this plan.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>Default/auth layouts are Tailwind-only, contain no shell blocker tags, and are responsive enough for the final Plan 07 build gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated user state -> navigation | Role determines visible shell links. |
| public settings -> announcement UI | Announcement text crosses from API data into shell display. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Elevation of Privilege | Role-aware nav | mitigate | Preserve existing role switch logic exactly; styling migration must not add links to wrong roles. |
| T-01-05-02 | Spoofing | Profile dropdown | mitigate | Preserve existing auth store identity display and logout action. |
| T-01-05-03 | Denial of Service | Mobile drawer | mitigate | Use SSR-safe viewport helper and bounded overlay state. |
| T-01-05-SC | Tampering | npm installs | accept | No package install in this plan. |
</threat_model>

<verification>
Run from `client/`: `npm run typecheck`. Plan 07 runs lint/typecheck/build after package/config removal.
</verification>

<success_criteria>
The main app shell and auth shell no longer depend on Vuetify, preserve calendar-first teacher navigation, and leave page redesign to later phases.
</success_criteria>

<output>
Create `.planning/phases/01-styling-platform-cutover/01-06-SUMMARY.md` when done.
</output>
