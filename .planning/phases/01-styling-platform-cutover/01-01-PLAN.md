---
phase: 01-styling-platform-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/components/AppIcon.vue
  - client/app/composables/useViewport.ts
  - client/app/components/AppToast.vue
  - client/app/components/TablePager.vue
  - client/app/components/LanguageSwitcher.vue
autonomous: true
requirements:
  - STYLE-03
  - STYLE-04
requirements_addressed:
  - STYLE-03
  - STYLE-04
must_haves:
  truths:
    - "Icon rendering works through Tailwind/Vue code and retained Material Design Icons per D-07, D-08, D-09."
    - "Globally mounted shared surfaces no longer require Vuetify primitives per D-02."
    - "Pagination and language switching remain usable after module removal."
  artifacts:
    - path: "client/app/components/AppIcon.vue"
      provides: "Lightweight Material Design Icon renderer"
      contains: "mdi"
    - path: "client/app/composables/useViewport.ts"
      provides: "Tailwind breakpoint state replacing Vuetify display composable"
      exports: ["useViewport"]
    - path: "client/app/components/AppToast.vue"
      provides: "Toast surface without Vuetify icon usage"
    - path: "client/app/components/TablePager.vue"
      provides: "Pagination controls without Vuetify select/pagination"
    - path: "client/app/components/LanguageSwitcher.vue"
      provides: "Language menu without Vuetify menu/list/button"
  key_links:
    - from: "client/app/components/AppToast.vue"
      to: "client/app/components/AppIcon.vue"
      via: "AppIcon renders toast icons"
      pattern: "AppIcon"
    - from: "client/app/components/LanguageSwitcher.vue"
      to: "client/app/components/AppIcon.vue"
      via: "AppIcon renders dropdown indicator"
      pattern: "AppIcon"
---

<objective>
Create the minimum shared Tailwind/Vue primitives needed before removing the Vuetify module.

Purpose: Phase 1 needs a real cutover, but globally mounted shared surfaces must stop depending on Vuetify first per D-02.
Output: AppIcon, viewport helper, and Tailwind replacements for toast, pager, and language switcher.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-styling-platform-cutover/01-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/app/assets/css/main.css
@client/app/components/AppToast.vue
@client/app/components/TablePager.vue
@client/app/components/LanguageSwitcher.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add AppIcon and viewport helper</name>
  <files>client/app/components/AppIcon.vue, client/app/composables/useViewport.ts</files>
  <action>Create `AppIcon.vue` per D-07, D-08, and D-09. It must render existing Material Design Icon names such as `mdi-calendar-month-outline` with an `<i>` element using `@mdi/font`, accept `name`, `size`, `decorative`, `label`, and optional class props/attrs, and expose accessible labeling when not decorative. Create `useViewport.ts` to replace Vuetify display usage per D-02 with reactive breakpoint booleans based on `window.matchMedia`, including SSR-safe defaults and cleanup on unmount.</action>
  <verify>
    <automated>cd client; npm run typecheck</automated>
  </verify>
  <done>`AppIcon` renders `mdi-*` names without `v-icon`; `useViewport()` exposes `mdAndUp` for shell migration; typecheck passes or reports only pre-existing unrelated failures documented in the summary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace shared toast and language switcher Vuetify primitives</name>
  <files>client/app/components/AppToast.vue, client/app/components/LanguageSwitcher.vue</files>
  <action>Rewrite `AppToast.vue` icon usage to `AppIcon` and convert its scoped styles to plain CSS if still using SCSS syntax. Rewrite `LanguageSwitcher.vue` as a small Tailwind/plain Vue dropdown with button, menu, keyboard-safe Escape close, outside-click close, locale switching behavior preserved, and no product redesign per D-02 and D-03.</action>
  <verify>
    <automated>cd client; npm run typecheck</automated>
  </verify>
  <done>Toast close/action icons render through `AppIcon`; language switching still calls the existing i18n path; neither file contains unresolved Vuetify component tags.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace TablePager controls</name>
  <files>client/app/components/TablePager.vue</files>
  <action>Rewrite `TablePager.vue` with native `select` and Tailwind buttons while preserving the same `page` and `limit` models, `PaginationMeta` contract, showing-range text, per-page options, disabled boundary behavior, and total-page display. Keep it small because Phase 2 owns the full UI kit.</action>
  <verify>
    <automated>cd client; npm run typecheck</automated>
  </verify>
  <done>`TablePager` no longer needs Vuetify select/pagination controls and preserves model updates for existing pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input -> shared controls | Native select/button interactions update page state. |
| icon name -> DOM class | Existing icon names are rendered as CSS classes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | AppIcon class rendering | mitigate | Normalize icon names to `mdi` + `mdi-*` class pattern and never render raw HTML. |
| T-01-01-02 | Denial of Service | useViewport media listeners | mitigate | Register listeners only on client and remove them on unmount. |
| T-01-01-SC | Tampering | npm installs | accept | No package install in this plan. |
</threat_model>

<verification>
Run from `client/`: `npm run typecheck`. Full `npm run lint`, `npm run typecheck`, and `npm run build` are enforced in Plan 04 after the real cutover per D-13.
</verification>

<success_criteria>
Shared blocker components are Tailwind/Vue-only, icons still render through `@mdi/font`, and Plan 03 can migrate the shell without importing Vuetify.
</success_criteria>

<output>
Create `.planning/phases/01-styling-platform-cutover/01-01-SUMMARY.md` when done.
</output>
