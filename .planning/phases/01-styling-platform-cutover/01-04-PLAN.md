---
phase: 01-styling-platform-cutover
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - client/nuxt.config.ts
  - client/package.json
  - client/package-lock.json
  - client/vuetify.config.ts
  - client/app/assets/css/vuetify.settings.scss
  - .planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
requirements_addressed:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
must_haves:
  truths:
    - "Nuxt no longer registers the removed UI framework module or config per D-01 and STYLE-01."
    - "SCSS platform files are no longer part of frontend build or dependency surface per D-04, D-05, STYLE-02."
    - "`@mdi/font` remains installed and available per D-07 and STYLE-04."
    - "Remaining old UI usages are documented with route/component role, tags, styling, priority, owner phase, and risk notes per D-10, D-11, D-12."
    - "Frontend lint, typecheck, and build pass after the real cutover per D-13 and D-14."
  artifacts:
    - path: "client/nuxt.config.ts"
      provides: "Tailwind-only Nuxt styling platform config"
    - path: "client/package.json"
      provides: "Dependency surface without Vuetify/Sass packages and with @mdi/font retained"
    - path: "client/package-lock.json"
      provides: "Lockfile matching package cutover"
    - path: ".planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md"
      provides: "Detailed migration inventory for Phase 2/3/4 planning"
  key_links:
    - from: "client/nuxt.config.ts"
      to: "client/app/assets/css/main.css"
      via: "Nuxt CSS array keeps @mdi/font and Tailwind entry only"
      pattern: "main.css"
    - from: "client/package.json"
      to: "client/package-lock.json"
      via: "npm removal updates both dependency manifests"
      pattern: "lockfileVersion"
    - from: ".planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md"
      to: ".planning/ROADMAP.md"
      via: "Inventory explicitly maps follow-up work to Phase 2, Phase 3, and Phase 4"
      pattern: "Phase 2"
---

<objective>
Complete the real platform cutover and produce the migration inventory.

Purpose: Phase 1 is complete only when config/dependencies are Tailwind-only, icons still work, SCSS is gone from the platform surface, remaining old UI usage is documented, and client verification passes.
Output: Updated Nuxt/package config, removed obsolete Vuetify settings/config files, detailed inventory, and verification evidence.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-styling-platform-cutover/01-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/nuxt.config.ts
@client/package.json
@client/package-lock.json
@client/vuetify.config.ts
@client/app/assets/css/vuetify.settings.scss
@client/app/assets/css/main.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove Vuetify and Sass platform config/dependencies</name>
  <files>client/nuxt.config.ts, client/package.json, client/package-lock.json, client/vuetify.config.ts, client/app/assets/css/vuetify.settings.scss</files>
  <action>Perform the real cutover per D-01. Remove the Nuxt module registration and related config block, remove obsolete config/settings files, and update package manifests to remove the UI framework module and Sass compiler packages while retaining `@mdi/font` per D-07. Use `npm uninstall` or equivalent package-manager action from `client/` so `package-lock.json` stays consistent. Do not add a temporary bridge and do not switch icon libraries per D-09.</action>
  <verify>
    <automated>cd client; npm run typecheck</automated>
  </verify>
  <done>Nuxt config, package manifest, and lockfile no longer include the removed platform packages/config; `@mdi/font` remains in dependencies.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create detailed migration inventory</name>
  <files>.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md</files>
  <action>Create the inventory required by D-10, D-11, and D-12 at `.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md`. Scan `client/app` for remaining old UI component tags, imports from the removed framework, direct SCSS usage, scoped SCSS usage, and framework utility classes. For each row include file path, route/component role, tags/patterns used, SCSS/import status, priority bucket, likely owning phase, and risky replacement notes. Include summary sections for Phase 2 shared UI kit needs, Phase 3 app shell/shared blockers, Phase 4 priority pages, and deferred lower-traffic pages. Note known starting scale from context: about 59 Vue files and about 1135 tag usages, then replace with the actual post-cutover scan results.</action>
  <verify>
    <automated>Test-Path .planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md</automated>
  </verify>
  <done>Inventory is detailed markdown, not a simple list; it guides later phases and explicitly avoids implementing deferred redesign work in Phase 1 per D-06.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run cutover gates and fix blockers</name>
  <files>client/nuxt.config.ts, client/package.json, client/package-lock.json, client/app/**/*.vue, client/app/**/*.ts, client/app/assets/css/main.css, .planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md</files>
  <action>Run Phase 1 verification per D-13. First run targeted scans proving config/dependency/platform removal and `@mdi/font` retention. Then run `npm run lint`, `npm run typecheck`, and `npm run build` from `client/`. If failures are caused by remaining dependency on the removed platform or Sass, fix them in Phase 1 per D-14. If a command cannot run because of environment/tooling limits, record the exact reason and closest verification performed per D-15. Backend verification is not required unless backend files were touched.</action>
  <verify>
    <automated>cd client; npm run lint; npm run typecheck; npm run build</automated>
  </verify>
  <done>Frontend lint/typecheck/build pass after the real cutover, or the summary records an environment-only blocker with exact command output and closest completed checks; no backend command is run unless server files changed.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Remove Vuetify/SCSS platform surface and establish Tailwind only | Plans 01-04 |
| REQ | STYLE-01 remove Nuxt registration/config | Plan 04 |
| REQ | STYLE-02 remove SCSS app styles | Plans 02, 04 |
| REQ | STYLE-03 Tailwind/CSS variable/component styling | Plans 01, 02, 03 |
| REQ | STYLE-04 icons continue without Vuetify | Plans 01, 03, 04 |
| CONTEXT | D-01 real cutover | Plan 04 Task 1 |
| CONTEXT | D-02 minimum blockers only | Plans 01, 03 |
| CONTEXT | D-03 no broad page redesign | Plans 02, 03 |
| CONTEXT | D-04 remove global SCSS import | Plan 02 Task 1 |
| CONTEXT | D-05 convert directly imported SCSS blockers | Plan 02 Task 2 |
| CONTEXT | D-06 document remaining non-blocking work | Plan 04 Task 2 |
| CONTEXT | D-07 keep @mdi/font | Plans 01, 04 |
| CONTEXT | D-08 add AppIcon | Plan 01 Task 1 |
| CONTEXT | D-09 no new icon library | Plans 01, 04 |
| CONTEXT | D-10 detailed markdown inventory | Plan 04 Task 2 |
| CONTEXT | D-11 inventory fields | Plan 04 Task 2 |
| CONTEXT | D-12 guide later phases | Plan 04 Task 2 |
| CONTEXT | D-13 frontend verification | Plan 04 Task 3 |
| CONTEXT | D-14 no dependency-caused failures | Plan 04 Task 3 |
| CONTEXT | D-15 exact verification limitation notes | Plan 04 Task 3 |
| RESEARCH | No RESEARCH.md exists; research disabled by init | Excluded |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package manifest -> supply chain | Dependency removal changes installed runtime surface. |
| build config -> client runtime | Nuxt config controls global CSS, modules, and app bootstrap. |
| inventory -> future planning | Inventory drives later work and must not hide blockers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | package-lock.json | mitigate | Use npm from `client/` to update manifest and lockfile together. |
| T-01-04-02 | Denial of Service | Nuxt bootstrap | mitigate | Run lint/typecheck/build after config removal and fix platform-caused failures. |
| T-01-04-03 | Repudiation | Migration inventory | mitigate | Inventory must include scan method, timestamp, counts, and owning phase for each bucket. |
| T-01-04-SC | Tampering | npm installs | accept | This plan removes packages only; no new package legitimacy audit required. |
</threat_model>

<verification>
From `client/` run:
- `npm run lint`
- `npm run typecheck`
- `npm run build`

Targeted gates:
- Nuxt config has no removed module registration or config block.
- Package manifest and lockfile do not keep removed platform packages.
- `@mdi/font` remains available.
- No directly compiled app style requires Sass.
- Backend verification: not required unless backend files are touched.
</verification>

<success_criteria>
Phase 1 succeeds when Tailwind is the only styling foundation in Nuxt/package config, Material Design Icons still render through `AppIcon`, the app shell/shared blockers are Tailwind-only, the inventory exists at `.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md`, and client lint/typecheck/build pass.
</success_criteria>

<output>
Create `.planning/phases/01-styling-platform-cutover/01-04-SUMMARY.md` when done.
</output>
