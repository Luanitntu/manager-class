---
phase: 01-styling-platform-cutover
plan: 07
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
  - 01-05
  - 01-06
files_modified:
  - client/nuxt.config.ts
  - client/package.json
  - client/package-lock.json
  - client/vuetify.config.ts
  - client/app/assets/css/vuetify.settings.scss
  - .planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md
autonomous: true
requirements:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
requirements_addressed:
  - STYLE-01
  - STYLE-02
  - STYLE-03
  - STYLE-04
must_haves:
  truths:
    - "Nuxt no longer registers the removed UI framework module or config per D-01 and STYLE-01."
    - "SCSS platform files are no longer part of frontend build or dependency surface per D-04, D-05, STYLE-02."
    - "`@mdi/font` remains installed and available per D-07 and STYLE-04."
    - "Remaining old UI usages are documented with route/component role, tags, styling, priority, owner phase, and risk notes per D-10, D-11, D-12."
    - "Frontend lint, typecheck, and build pass after the real cutover per D-13 and D-14."
  artifacts:
    - path: "client/nuxt.config.ts"
      provides: "Tailwind-only Nuxt styling platform config"
    - path: "client/package.json"
      provides: "Dependency surface without Vuetify/Sass packages and with @mdi/font retained"
    - path: "client/package-lock.json"
      provides: "Lockfile matching package cutover"
    - path: ".planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md"
      provides: "Detailed migration inventory for Phase 2/3/4 planning"
  key_links:
    - from: "client/nuxt.config.ts"
      to: "client/app/assets/css/main.css"
      via: "Nuxt CSS array keeps @mdi/font and Tailwind entry only"
      pattern: "main.css"
    - from: "client/package.json"
      to: "client/package-lock.json"
      via: "npm removal updates both dependency manifests"
      pattern: "lockfileVersion"
    - from: ".planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md"
      to: ".planning/ROADMAP.md"
      via: "Inventory explicitly maps follow-up work to Phase 2, Phase 3, and Phase 4"
      pattern: "Phase 2"
---

<objective>
Complete the real platform cutover, create the detailed inventory, and run fail-fast verification.

Purpose: Phase 1 is complete only when config/dependencies are Tailwind-only, icons still work, SCSS is gone from the platform surface, remaining old UI usage is documented, and client verification passes.
Output: Updated Nuxt/package config, removed obsolete Vuetify settings/config files, detailed inventory, and verification evidence.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-styling-platform-cutover/01-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/nuxt.config.ts
@client/package.json
@client/package-lock.json
@client/vuetify.config.ts
@client/app/assets/css/vuetify.settings.scss
@client/app/assets/css/main.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove Vuetify and Sass platform config/dependencies</name>
  <files>client/nuxt.config.ts, client/package.json, client/package-lock.json, client/vuetify.config.ts, client/app/assets/css/vuetify.settings.scss</files>
  <action>Perform the real cutover per D-01. Remove the Nuxt module registration and related config block, remove obsolete config/settings files, and update package manifests to remove the UI framework module and Sass compiler packages while retaining `@mdi/font` per D-07. Preserve the Material Design Icons CSS load path by keeping an `@mdi/font/css/materialdesignicons*.css` entry in the Nuxt `css` array or an equivalent import from `client/app/assets/css/main.css`; package retention alone is not enough for STYLE-04. Use `npm uninstall` or equivalent package-manager action from `client/` so `package-lock.json` stays consistent. Do not add a temporary bridge and do not switch icon libraries per D-09.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>Nuxt config, package manifest, and lockfile no longer include the removed platform packages/config; `@mdi/font` remains in dependencies and `@mdi/font/css/materialdesignicons*.css` remains loaded through the Nuxt `css` array or `main.css`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create detailed migration inventory</name>
  <files>.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md</files>
  <action>Create the inventory required by D-10, D-11, and D-12 at `.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md`. Scan `client/app` for remaining old UI component tags, imports from the removed framework, direct SCSS usage, scoped SCSS usage, and framework utility classes. For each row include file path, route/component role, tags/patterns used, SCSS/import status, priority bucket, likely owning phase, and risky replacement notes. Include required sections for scan method and timestamp, summary counts, Phase 2 shared UI kit needs, Phase 3 app shell/shared blockers, Phase 4 priority pages, and deferred lower-traffic pages. Note known starting scale from context: about 59 Vue files and about 1135 tag usages, then replace with actual post-cutover scan results. Do not implement non-blocking v-prefixed component replacement or visual/page redesign in this plan per D-03 and D-06.</action>
  <verify>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path '.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md' -Pattern 'File Path.*Route/Component Role.*Tags/Patterns.*SCSS/Import Status.*Priority Bucket.*Likely Owning Phase.*Risky Replacement Notes' -Quiet)) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path '.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md' -Pattern 'Phase 2' -Quiet)) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path '.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md' -Pattern 'Phase 3' -Quiet)) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path '.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md' -Pattern 'Phase 4' -Quiet)) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path '.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md' -Pattern '^\|\s*client/app/' -Quiet)) { exit 1 }"</automated>
  </verify>
  <done>Inventory has the required columns, Phase 2/3/4 guidance sections, scan method/counts, and at least one concrete `client/app` row when scans find remaining old UI usage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run cutover gates and report unexpected blockers</name>
  <files>client/nuxt.config.ts, client/package.json, client/package-lock.json, .planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md</files>
  <action>Run Phase 1 verification per D-13 with fail-fast commands. First run executable targeted scans proving config/dependency/platform removal and `@mdi/font` retention. Then run `npm run lint`, `npm run typecheck`, and `npm run build` from `client/` as separate fail-fast automated commands. This task is scan-and-verify only for app-wide Vue/TS/CSS files: do not edit app source outside the explicit final-plan file list. If scans or commands fail because of blockers in files owned by Plans 01-06, stop and record a planning gap/revision note in the required summary with exact file paths, failing command, and likely owning plan; do not perform broad app-wide repair. If failures are limited to this plan's config/package/inventory files, fix those files and rerun the failing command. If a command cannot run because of environment/tooling limits, record the exact reason and closest verification performed per D-15. Backend verification is not required unless backend files were touched.</action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Get-ChildItem client -Recurse -File -Include *.ts,*.vue,*.json,*.css | Where-Object { $_.FullName -notmatch '\\node_modules\\|\\.nuxt\\|\\.output\\|\\dist\\' } | Select-String -Pattern 'vuetify-nuxt-module|vuetify:|vuetify.config.ts' -Quiet) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (Test-Path client/vuetify.config.ts) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (Test-Path client/app/assets/css/vuetify.settings.scss) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (Select-String -Path client/package.json,client/package-lock.json -Pattern 'sass-embedded' -Quiet) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command 'if (Get-ChildItem client/app -Recurse -File -Include *.vue,*.ts,*.css | Where-Object { $_.FullName -notmatch "\\node_modules\\|\\.nuxt\\|\\.output\\|\\dist\\" } | Select-String -Pattern "lang\s*=\s*[''""]scss[''""]|\.scss\b|@use\b|@forward\b" -Quiet) { exit 1 }'</automated>
    <automated>powershell -NoProfile -Command "if (Get-ChildItem client/app -Recurse -File -Include *.vue,*.ts | Select-String -Pattern 'useDisplay|from ''vuetify''' -Quiet) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command 'if (-not (Select-String -Path client/package.json -Pattern "@mdi/font" -Quiet)) { exit 1 }; if (-not (Select-String -Path client/nuxt.config.ts,client/app/assets/css/main.css -Pattern "@mdi/font/css/materialdesignicons.*\.css" -Quiet)) { exit 1 }'</automated>
    <automated>powershell -NoProfile -Command "if (Select-String -Path client/app/layouts/default.vue,client/app/layouts/auth.vue,client/app/components/AppToast.vue,client/app/components/TablePager.vue,client/app/components/LanguageSwitcher.vue -Pattern ([char]60 + 'v-[A-Za-z]') -Quiet) { exit 1 }"</automated>
    <automated>npm --prefix client run lint</automated>
    <automated>npm --prefix client run typecheck</automated>
    <automated>npm --prefix client run build</automated>
  </verify>
  <done>Frontend lint/typecheck/build pass after the real cutover; targeted scans prove removed platform packages/config/imports are absent, `@mdi/font` is retained with the Material Design Icons CSS load path, SCSS build-path syntax is absent, and blocker files contain no Vuetify component tags.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Remove Vuetify/SCSS platform surface and establish Tailwind only | Plans 01-07 |
| REQ | STYLE-01 remove Nuxt registration/config | Plan 07 |
| REQ | STYLE-02 remove SCSS app styles | Plans 02, 03, 04, 05, 07 |
| REQ | STYLE-03 Tailwind/CSS variable/component styling | Plans 01, 02, 03, 04, 05, 06 |
| REQ | STYLE-04 icons continue without Vuetify | Plans 01, 06, 07 |
| CONTEXT | D-01 real cutover | Plan 07 Task 1 |
| CONTEXT | D-02 minimum blockers only | Plans 01, 06 |
| CONTEXT | D-03 no broad page redesign | Plans 02, 03, 04, 05, 06 |
| CONTEXT | D-04 remove global SCSS import | Plan 02 Task 1 |
| CONTEXT | D-05 convert directly imported SCSS blockers | Plans 02, 03, 04, 05 |
| CONTEXT | D-06 document remaining non-blocking work | Plan 07 Task 2 |
| CONTEXT | D-07 keep @mdi/font | Plans 01, 07 |
| CONTEXT | D-08 add AppIcon | Plan 01 Task 1 |
| CONTEXT | D-09 no new icon library | Plans 01, 07 |
| CONTEXT | D-10 detailed markdown inventory | Plan 07 Task 2 |
| CONTEXT | D-11 inventory fields | Plan 07 Task 2 |
| CONTEXT | D-12 guide later phases | Plan 07 Task 2 |
| CONTEXT | D-13 frontend verification | Plan 07 Task 3 |
| CONTEXT | D-14 no dependency-caused failures | Plan 07 Task 3 |
| CONTEXT | D-15 exact verification limitation notes | Plan 07 Task 3 |
| RESEARCH | No RESEARCH.md exists; research disabled by init | Excluded |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package manifest -> supply chain | Dependency removal changes installed runtime surface. |
| build config -> client runtime | Nuxt config controls global CSS, modules, and app bootstrap. |
| inventory -> future planning | Inventory drives later work and must not hide blockers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Tampering | package-lock.json | mitigate | Use npm from `client/` to update manifest and lockfile together. |
| T-01-07-02 | Denial of Service | Nuxt bootstrap | mitigate | Run targeted scans plus lint/typecheck/build after config removal; stop and report planning gaps for blockers outside this plan's explicit files. |
| T-01-07-03 | Repudiation | Migration inventory | mitigate | Inventory must include scan method, timestamp, counts, required columns, and owning phase for each bucket. |
| T-01-07-SC | Tampering | npm installs | accept | This plan removes packages only; no new package legitimacy audit required. |
</threat_model>

<verification>
Automated verification is the full `<verify>` list in Task 3: executable removal scans, `@mdi/font` retention scan, blocker-file tag scan, and separate fail-fast `npm run lint`, `npm run typecheck`, and `npm run build` commands from `client/`.
</verification>

<success_criteria>
Phase 1 succeeds when Tailwind is the only styling foundation in Nuxt/package config, Material Design Icons still render through `AppIcon`, the app shell/shared blockers are Tailwind-only, the inventory exists with required Phase 2/3/4 guidance, and client lint/typecheck/build pass.
</success_criteria>

<output>
Create `.planning/phases/01-styling-platform-cutover/01-07-SUMMARY.md` when done.
</output>
