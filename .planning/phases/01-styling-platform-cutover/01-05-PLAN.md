---
phase: 01-styling-platform-cutover
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - client/app/styles/calendar/page.scss
  - client/app/styles/calendar/page.css
  - client/app/styles/calendar/board.scss
  - client/app/styles/calendar/board.css
  - client/app/styles/calendar/detail.scss
  - client/app/styles/calendar/detail.css
  - client/app/styles/calendar/header.scss
  - client/app/styles/calendar/header.css
  - client/app/components/calendar/TeacherSessionDetail.vue
  - client/app/components/calendar/TeacherCalendarHeader.vue
  - client/app/components/calendar/TeacherCalendarBoard.vue
  - client/app/components/calendar/TeacherCalendar.vue
  - client/app/components/calendar/StudentCalendar.vue
autonomous: true
requirements:
  - STYLE-02
  - STYLE-03
requirements_addressed:
  - STYLE-02
  - STYLE-03
must_haves:
  truths:
    - "Calendar build-path styles no longer require Sass per D-05."
    - "Teacher calendar remains the central workflow and keeps current visual intent per D-03."
    - "Non-blocking old UI or redesign work is routed to Plan 07 inventory per D-06."
  artifacts:
    - path: "client/app/styles/calendar/page.css"
      provides: "Plain CSS replacement for calendar page styles where still needed"
    - path: "client/app/styles/calendar/board.css"
      provides: "Plain CSS replacement for calendar board styles where still needed"
    - path: "client/app/styles/calendar/detail.css"
      provides: "Plain CSS replacement for calendar detail styles where still needed"
    - path: "client/app/styles/calendar/header.css"
      provides: "Plain CSS replacement for calendar header styles where still needed"
    - path: "client/app/components/calendar/TeacherCalendar.vue"
      provides: "Teacher calendar imports plain CSS or component-local CSS"
  key_links:
    - from: "client/app/components/calendar/TeacherCalendar.vue"
      to: "client/app/styles/calendar/page.css"
      via: "Calendar style import is plain CSS when external file remains"
      pattern: "page.css"
    - from: "client/app/components/calendar/TeacherCalendarBoard.vue"
      to: "client/app/styles/calendar/board.css"
      via: "Board style import is plain CSS when external file remains"
      pattern: "board.css"
---

<objective>
Convert only calendar SCSS build blockers to Tailwind-compatible CSS.

Purpose: Phase 1 must remove Sass from direct build-path calendar styles while preserving the calendar-first teacher workflow per D-03, D-05, and D-06.
Output: Plain CSS or component-local plain CSS for teacher/student calendar style imports.

Scope note: This plan intentionally touches 13 calendar-first files because the changes are paired style conversion/import updates within the same workflow slice; it does not redesign calendar behavior or page layout.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-styling-platform-cutover/01-CONTEXT.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@client/app/styles/calendar/page.scss
@client/app/styles/calendar/board.scss
@client/app/styles/calendar/detail.scss
@client/app/styles/calendar/header.scss
@client/app/components/calendar/TeacherCalendar.vue
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert shared calendar style files</name>
  <files>client/app/styles/calendar/page.scss, client/app/styles/calendar/page.css, client/app/styles/calendar/board.scss, client/app/styles/calendar/board.css, client/app/styles/calendar/detail.scss, client/app/styles/calendar/detail.css, client/app/styles/calendar/header.scss, client/app/styles/calendar/header.css</files>
  <action>Convert calendar SCSS files imported by calendar components to plain CSS per D-05. Flatten nesting, replace Sass-only syntax with valid CSS, preserve selectors/classes and calendar visual intent, and remove obsolete SCSS imports. Keep changes scoped to direct calendar Sass build blockers per D-06.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>Calendar external styles used by Nuxt are plain CSS or removed because equivalent component-local plain CSS exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update teacher calendar imports</name>
  <files>client/app/components/calendar/TeacherSessionDetail.vue, client/app/components/calendar/TeacherCalendarHeader.vue, client/app/components/calendar/TeacherCalendarBoard.vue, client/app/components/calendar/TeacherCalendar.vue</files>
  <action>Update teacher calendar SFC imports and style attributes to use plain CSS or component-local plain CSS. Preserve teacher calendar board, header, session detail, and daily workflow behavior; do not redesign calendar interactions or page layout per D-03.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>Teacher calendar SFCs no longer reference `.scss` or SCSS language attributes and keep calendar-first behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update student calendar imports and scan only calendar SCSS blockers</name>
  <files>client/app/components/calendar/StudentCalendar.vue, client/app/components/calendar/TeacherSessionDetail.vue, client/app/components/calendar/TeacherCalendarHeader.vue, client/app/components/calendar/TeacherCalendarBoard.vue, client/app/components/calendar/TeacherCalendar.vue</files>
  <action>Update `StudentCalendar.vue` imports/style attributes to use plain CSS or component-local plain CSS. Run a targeted scan over calendar files touched in this plan plus their direct style imports. Fix only SCSS-only direct build blockers in this calendar slice. Record any non-blocking v-prefixed component usage, visual consistency, or shared component needs for Plan 07 inventory per D-06, D-10, D-11, and D-12.</action>
  <verify>
    <automated>npm --prefix client run typecheck</automated>
  </verify>
  <done>Calendar files in this plan contain no direct SCSS build blockers; unrelated migration findings are ready for inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS selectors -> calendar DOM | Style migration changes teacher/student calendar presentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Denial of Service | Calendar build | mitigate | Remove Sass-only syntax from calendar build path before dependency removal. |
| T-01-04-02 | Tampering | Calendar workflow | mitigate | Preserve existing component markup/data flow and limit edits to styling imports/styles. |
| T-01-04-SC | Tampering | npm installs | accept | No package install in this plan. |
</threat_model>

<verification>
Run from `client/`: `npm run typecheck`. Full platform scans, lint, typecheck, and build are enforced in Plan 07.
</verification>

<success_criteria>
Teacher/student calendar SCSS blockers are converted to Tailwind-compatible CSS without redesigning calendar workflows or touching other surfaces.
</success_criteria>

<output>
Create `.planning/phases/01-styling-platform-cutover/01-05-SUMMARY.md` when done.
</output>
