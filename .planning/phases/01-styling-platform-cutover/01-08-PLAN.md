---
phase: 01-styling-platform-cutover
plan: 08
type: execute
wave: 4
depends_on:
  - 01-07
files_modified:
  - client/app/components/dashboard/StudentWorkspaceDashboard.vue
  - client/app/components/AppInitialLoader.vue
  - client/app/components/AppSkeleton.vue
  - client/app/components/StudentSchedule.vue
  - client/app/pages/student/documents.vue
  - client/app/pages/classes.vue
  - client/app/pages/documents.vue
  - client/app/pages/payments.vue
  - client/app/pages/reports.vue
  - client/app/pages/students.vue
  - .planning/phases/01-styling-platform-cutover/01-08-SUMMARY.md
autonomous: true
gap_closure: true
requirements:
  - STYLE-02
  - STYLE-03
must_haves:
  truths:
    - "The 10 Plan 07 blocker Vue files no longer contain scoped Sass language attributes or Sass-only markers, closing STYLE-02."
    - "Remaining styles in the blocker files are plain scoped CSS or Tailwind-compatible CSS while preserving existing visual intent, closing STYLE-03."
    - "Teacher/student documents typecheck no longer fails because `targetType` is narrowed to the literal union expected by the upload target."
    - "Frontend lint, typecheck, and build pass after Sass removal."
    - "Final scans prove Phase 1 did not regress STYLE-01 or STYLE-04 while closing STYLE-02 and STYLE-03."
  artifacts:
    - path: "client/app/components/dashboard/StudentWorkspaceDashboard.vue"
      provides: "Student dashboard styles converted from SCSS to plain scoped CSS"
    - path: "client/app/components/AppInitialLoader.vue"
      provides: "Initial loader styles converted from SCSS to plain scoped CSS"
    - path: "client/app/components/AppSkeleton.vue"
      provides: "Skeleton styles converted from SCSS to plain scoped CSS"
    - path: "client/app/components/StudentSchedule.vue"
      provides: "Student schedule styles converted from SCSS to plain scoped CSS"
    - path: "client/app/pages/student/documents.vue"
      provides: "Student documents styles converted from SCSS to plain scoped CSS"
    - path: "client/app/pages/classes.vue"
      provides: "Legacy classes page styles converted from SCSS to plain scoped CSS"
    - path: "client/app/pages/documents.vue"
      provides: "Teacher documents styles converted from SCSS to plain scoped CSS and upload target literal type fix"
    - path: "client/app/pages/payments.vue"
      provides: "Payments styles converted from SCSS to plain scoped CSS"
    - path: "client/app/pages/reports.vue"
      provides: "Reports styles converted from SCSS to plain scoped CSS"
    - path: "client/app/pages/students.vue"
      provides: "Legacy students page styles converted from SCSS to plain scoped CSS"
    - path: ".planning/phases/01-styling-platform-cutover/01-08-SUMMARY.md"
      provides: "Gap closure evidence and final verification result"
  key_links:
    - from: "client/app/pages/documents.vue"
      to: "client/app/composables"
      via: "Preserve existing composable/API flow; only narrow the upload target literal type"
      pattern: "targetType.*(CLASS|STUDENT)"
    - from: "client/package.json"
      to: "client/app/**/*.vue"
      via: "Sass compiler is removed, so every Vue style block must parse as plain CSS"
      pattern: "lang\\s*=\\s*['\\\"]scss['\\\"]"
    - from: "client/nuxt.config.ts"
      to: "client/app/assets/css/main.css"
      via: "Final regression scan keeps Vuetify/Sass removed and MDI CSS retained"
      pattern: "@mdi/font/css/materialdesignicons"
---

<objective>
Close the Phase 1 Plan 07 verification blockers without expanding into Phase 2/3/4 redesign.

Purpose: Phase 1 cannot finish while Sass is removed but 10 Vue files still require Sass parsing, and typecheck still fails in teacher documents upload target typing.
Output: The 10 listed Vue files use plain scoped CSS/Tailwind-compatible CSS, `documents.vue` narrows `targetType` correctly, and client lint/typecheck/build pass.
</objective>

<execution_context>
@C:/code/manager-class/.codex/gsd-core/workflows/execute-plan.md
@C:/code/manager-class/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-styling-platform-cutover/01-CONTEXT.md
@.planning/phases/01-styling-platform-cutover/01-07-SUMMARY.md
@.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert shared/student SCSS blockers to plain scoped CSS</name>
  <read_first>
    @.planning/phases/01-styling-platform-cutover/01-07-SUMMARY.md
    @.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md
    @.planning/phases/01-styling-platform-cutover/01-03-SUMMARY.md
    @.planning/phases/01-styling-platform-cutover/01-04-SUMMARY.md
    @.planning/phases/01-styling-platform-cutover/01-05-SUMMARY.md
    @client/app/components/dashboard/StudentWorkspaceDashboard.vue
    @client/app/components/AppInitialLoader.vue
    @client/app/components/AppSkeleton.vue
    @client/app/components/StudentSchedule.vue
    @client/app/pages/student/documents.vue
  </read_first>
  <files>client/app/components/dashboard/StudentWorkspaceDashboard.vue, client/app/components/AppInitialLoader.vue, client/app/components/AppSkeleton.vue, client/app/components/StudentSchedule.vue, client/app/pages/student/documents.vue</files>
  <action>Convert only the scoped style blocks in these five files from SCSS to plain CSS or Tailwind-compatible CSS. Preserve the current templates, script logic, data/composable flow, Vuetify markup, visual hierarchy, responsive behavior, loading/empty states, and student-facing behavior. Flatten nested selectors, replace Sass variables/mixins/functions with equivalent CSS literals or existing CSS custom properties, and keep Vue scoped selectors such as `:deep()` only when needed. Remove the Sass language attribute from each converted block. Do not replace remaining `<v-*>` usage unless it is strictly required for CSS parsing; Phase 2/3/4 own broad component migration per D-03 and D-06. This task implements STYLE-02 and STYLE-03 by removing direct Sass syntax while preserving existing visual intent.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$files=@('client/app/components/dashboard/StudentWorkspaceDashboard.vue','client/app/components/AppInitialLoader.vue','client/app/components/AppSkeleton.vue','client/app/components/StudentSchedule.vue','client/app/pages/student/documents.vue'); if (Select-String -Path $files -Pattern 'lang\s*=\s*[''\""]scss[''\""]|@use\b|@forward\b|@mixin\b|@include\b|\$[A-Za-z_-]' -Quiet) { exit 1 }"</automated>
  </verify>
  <done>The five shared/student blocker files no longer require Sass parsing and retain existing user-visible behavior and layout intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert teacher route SCSS blockers and narrow documents upload target</name>
  <read_first>
    @.planning/phases/01-styling-platform-cutover/01-07-SUMMARY.md
    @.planning/phases/01-styling-platform-cutover/01-MIGRATION-INVENTORY.md
    @.planning/phases/01-styling-platform-cutover/01-02-SUMMARY.md
    @client/app/pages/classes.vue
    @client/app/pages/documents.vue
    @client/app/pages/payments.vue
    @client/app/pages/reports.vue
    @client/app/pages/students.vue
  </read_first>
  <files>client/app/pages/classes.vue, client/app/pages/documents.vue, client/app/pages/payments.vue, client/app/pages/reports.vue, client/app/pages/students.vue</files>
  <behavior>
    - Typecheck fails before the documents fix with `client/app/pages/documents.vue(108,56)` because `targetType` widens to `string`.
    - Typecheck passes after the documents fix because upload target objects use literal `"CLASS" | "STUDENT"` values, not a widened string.
    - Plain CSS parsing passes after converting the five route style blocks because no Sass language attribute or Sass-only marker remains.
  </behavior>
  <action>Convert the scoped style blocks in the five listed teacher route files from SCSS to plain CSS or Tailwind-compatible CSS using the same conservative pattern as Plans 02-05. Preserve existing templates, Vuetify markup, composables, API calls, filters, dialogs, forms, lists, empty/loading/error states, and visual intent. In `client/app/pages/documents.vue`, fix the Plan 07 typecheck blocker by narrowing the upload target `targetType` value to the existing literal union expected by the relevant composable/component contract; prefer `as const`, a typed helper, or an explicit union annotation at the target-object creation point. Do not introduce `any`, do not loosen downstream types, and do not move API calls into the page. This task implements STYLE-02 and STYLE-03 and closes the known typecheck gap while staying inside Phase 1 cutover scope.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$files=@('client/app/pages/classes.vue','client/app/pages/documents.vue','client/app/pages/payments.vue','client/app/pages/reports.vue','client/app/pages/students.vue'); if (Select-String -Path $files -Pattern 'lang\s*=\s*[''\""]scss[''\""]|@use\b|@forward\b|@mixin\b|@include\b|\$[A-Za-z_-]' -Quiet) { exit 1 }"</automated>
    <automated>npm.cmd --prefix client run typecheck</automated>
  </verify>
  <done>The five teacher route files no longer require Sass parsing, `documents.vue` no longer widens `targetType`, and frontend typecheck passes or reports only newly discovered unrelated blockers with exact paths in the summary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run final Phase 1 gap closure verification</name>
  <read_first>
    @.planning/phases/01-styling-platform-cutover/01-07-SUMMARY.md
    @.planning/phases/01-styling-platform-cutover/01-CONTEXT.md
    @.planning/REQUIREMENTS.md
    @client/package.json
    @client/nuxt.config.ts
    @client/app/assets/css/main.css
  </read_first>
  <files>.planning/phases/01-styling-platform-cutover/01-08-SUMMARY.md</files>
  <action>Run the full gap closure verification on Windows. First run targeted scans for all 10 original blocker files and the full client app to prove no Sass language attributes, `.scss` imports, `@use`, or `@forward` remain in active frontend source. Then verify the Phase 1 platform surface did not regress: no `vuetify-nuxt-module`, no `sass-embedded`, no `client/vuetify.config.ts`, no `client/app/assets/css/vuetify.settings.scss`, no `useDisplay` or direct imports from the removed UI framework, and `@mdi/font` plus Material Design Icons CSS remain loaded. Finally run `npm.cmd --prefix client run lint`, `npm.cmd --prefix client run typecheck`, and `npm.cmd --prefix client run build`. Create `.planning/phases/01-styling-platform-cutover/01-08-SUMMARY.md` with commands, pass/fail status, changed files, and any remaining blocker paths. Do not edit backend files; record backend verification as not run because this plan is frontend-only.</action>
  <verify>
    <automated>powershell -NoProfile -Command "$files=@('client/app/components/dashboard/StudentWorkspaceDashboard.vue','client/app/components/AppInitialLoader.vue','client/app/components/AppSkeleton.vue','client/app/components/StudentSchedule.vue','client/app/pages/student/documents.vue','client/app/pages/classes.vue','client/app/pages/documents.vue','client/app/pages/payments.vue','client/app/pages/reports.vue','client/app/pages/students.vue'); if (Select-String -Path $files -Pattern 'lang\s*=\s*[''\""]scss[''\""]|@use\b|@forward\b|@mixin\b|@include\b|\$[A-Za-z_-]' -Quiet) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$hits = Get-ChildItem client/app -Recurse -File -Include *.vue,*.ts,*.css | Where-Object { $_.FullName -notmatch '\\node_modules\\|\\.nuxt\\|\\.output\\|\\dist\\' } | Select-String -Pattern 'lang\s*=\s*[''\""]scss[''\""]|\.scss\b|@use\b|@forward\b'; if ($hits) { $hits | Select-Object Path,LineNumber,Line; exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (Test-Path client/vuetify.config.ts) { exit 1 }; if (Test-Path client/app/assets/css/vuetify.settings.scss) { exit 1 }; if (Select-String -Path client/package.json,client/package-lock.json -Pattern 'vuetify-nuxt-module|sass-embedded' -Quiet) { exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "$hits = Get-ChildItem client/app -Recurse -File -Include *.vue,*.ts | Where-Object { $_.FullName -notmatch '\\node_modules\\|\\.nuxt\\|\\.output\\|\\dist\\' } | Select-String -Pattern 'useDisplay|from [''\""]vuetify[''\""]'; if ($hits) { $hits | Select-Object Path,LineNumber,Line; exit 1 }"</automated>
    <automated>powershell -NoProfile -Command "if (-not (Select-String -Path client/package.json -Pattern '@mdi/font' -Quiet)) { exit 1 }; if (-not (Select-String -Path client/nuxt.config.ts,client/app/assets/css/main.css -Pattern '@mdi/font/css/materialdesignicons.*\.css' -Quiet)) { exit 1 }"</automated>
    <automated>npm.cmd --prefix client run lint</automated>
    <automated>npm.cmd --prefix client run typecheck</automated>
    <automated>npm.cmd --prefix client run build</automated>
    <automated>powershell -NoProfile -Command "if (-not (Test-Path '.planning/phases/01-styling-platform-cutover/01-08-SUMMARY.md')) { exit 1 }; if (-not (Select-String -Path '.planning/phases/01-styling-platform-cutover/01-08-SUMMARY.md' -Pattern 'npm.cmd --prefix client run lint|npm.cmd --prefix client run typecheck|npm.cmd --prefix client run build' -Quiet)) { exit 1 }"</automated>
  </verify>
  <done>All targeted scans pass, frontend lint/typecheck/build pass, no backend files are touched, and the summary records final gap closure evidence.</done>
</task>

</tasks>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Remove the Vuetify/SCSS platform surface and establish Tailwind as the only styling foundation | Task 1, Task 2, Task 3 close remaining Sass blockers after Plan 07 removed dependencies/config. |
| REQ STYLE-01 | Nuxt no longer registers Vuetify config/module | Task 3 regression scan only; primary implementation already covered by Plan 07. |
| REQ STYLE-02 | Frontend no longer imports SCSS application styles | Task 1 and Task 2 convert the 10 Plan 07 blocker files; Task 3 full scan proves closure. |
| REQ STYLE-03 | Remaining styling uses Tailwind CSS, CSS variables, and Vue component classes | Task 1 and Task 2 require plain scoped CSS/Tailwind-compatible CSS with visual parity. |
| REQ STYLE-04 | Icons continue without Vuetify components | Task 3 regression scan only; primary implementation already covered by Plans 01, 06, and 07. |
| CONTEXT D-01 | Real cutover in Phase 1 | Task 3 verifies no Sass/Vuetify dependency regression after gap closure. |
| CONTEXT D-02 | Replace only minimum blockers | Task 1 and Task 2 prohibit broad `<v-*>` migration unless required for CSS parsing. |
| CONTEXT D-03 | Avoid product page redesign in Phase 1 | Task 1 and Task 2 preserve templates, behavior, and visual intent. |
| CONTEXT D-04 | Remove global SCSS imports | Task 3 regression scan verifies no app SCSS import remains. |
| CONTEXT D-05 | Convert directly imported/current build-path SCSS blockers | Task 1 and Task 2 convert all 10 Plan 07 blocker files. |
| CONTEXT D-06 | Document remaining non-blocking style work | Existing Plan 07 inventory remains source of truth; Task 3 summary records any remaining blocker paths. |
| CONTEXT D-07 | Keep `@mdi/font` | Task 3 verifies dependency and CSS load path remain. |
| CONTEXT D-08 | Add lightweight icon primitive | Existing Plan 01 artifact; Task 3 verifies no regression to icon CSS availability. |
| CONTEXT D-09 | Do not switch icon library | No task changes icon library; Task 3 verifies `@mdi/font` remains. |
| CONTEXT D-10 | Detailed markdown inventory | Existing Plan 07 artifact read as context; no inventory rewrite in this gap closure. |
| CONTEXT D-11 | Inventory fields | Existing Plan 07 artifact read as context; no inventory rewrite in this gap closure. |
| CONTEXT D-12 | Inventory guides later phases | Existing Plan 07 artifact read as context; this gap closure stays Phase 1-only. |
| CONTEXT D-13 | Phase 1 lint/typecheck/build pass | Task 3 runs all three final client commands. |
| CONTEXT D-14 | Dependency-caused failures not acceptable | Task 3 build scan/gate proves Sass removal no longer breaks the build. |
| CONTEXT D-15 | Record exact environment limitation if verification cannot run | Task 3 summary action requires exact reason and closest verification. |
| SUMMARY 01-07 | 10 files still contain SCSS markers | Task 1 and Task 2 cover all 10 files. |
| SUMMARY 01-07 | `documents.vue(108,56)` target type widens to string | Task 2 narrows to literal union. |
| SUMMARY 01-07 | lint/typecheck/build need rerun | Task 3 runs all three. |
| RESEARCH | Research skipped by orchestrator | Excluded. |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vue SFC style parser -> build pipeline | Sass compiler was removed; any remaining Sass syntax can deny builds. |
| Route/component code -> API composables | Pages must preserve existing composable calls and avoid direct API rewiring. |
| Client build config -> runtime UI | Regression scans protect removed dependency/config surface and icon CSS availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08-01 | Denial of Service | Nuxt build | mitigate | Convert all 10 Sass blocker files and run full app Sass-marker scan plus `npm.cmd --prefix client run build`. |
| T-01-08-02 | Tampering | `client/app/pages/documents.vue` upload target | mitigate | Narrow `targetType` at creation point without loosening downstream types or using `any`; prove with `npm.cmd --prefix client run typecheck`. |
| T-01-08-03 | Information Disclosure | frontend data flow | mitigate | Preserve existing composables/API contracts and do not introduce direct API calls or tenant-scope changes. |
| T-01-08-04 | Denial of Service | icon/runtime CSS | mitigate | Regression scan verifies `@mdi/font` and Material Design Icons CSS load path remain after Sass conversion. |
| T-01-08-SC | Tampering | npm package surface | accept | No package install or dependency addition is planned; package scan only verifies Plan 07 removals remain. |
</threat_model>

<verification>
Overall verification is Task 3: targeted Sass scans for the 10 blocker files, full client app Sass-marker scan, Vuetify/Sass package/config regression scan, icon retention scan, and `npm.cmd --prefix client run lint`, `npm.cmd --prefix client run typecheck`, `npm.cmd --prefix client run build`.
</verification>

<success_criteria>
Phase 1 gap closure succeeds when all 10 Plan 07 blocker files are plain CSS/Tailwind-compatible, `client/app/pages/documents.vue` no longer widens `targetType`, final client lint/typecheck/build pass, scans show no active frontend SCSS markers, and STYLE-01/STYLE-04 regression scans still pass.
</success_criteria>

<output>
Create `.planning/phases/01-styling-platform-cutover/01-08-SUMMARY.md` when done.
</output>
